---
phase: 05-mcp-documentation
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]
files_modified:
  - src/lib/mcp/catalog.ts
  - src/lib/__tests__/mcp-catalog.test.ts
  - src/lib/site-content.ts
  - src/components/site-primitives.tsx
  - src/locales/site/docs/en.ts
  - src/locales/site/docs/pt.ts
  - src/locales/site/docs/es.ts
  - src/locales/site/docs/fr.ts
  - src/locales/site/docs/de.ts
  - src/locales/site/docs/zh.ts
  - src/locales/site/docs/ja.ts

must_haves:
  truths:
    - "Navigating /docs/mcp in any of the 7 locales (en, pt, es, fr, de, zh, ja) renders without error and shows the POST /api/mcp endpoint and a 25-tool catalog (DOCS-01, D-01, D-06)"
    - "The catalog tool-name set is exactly the 25 names registered by registerAllTools — no missing, no phantom — enforced by an automated set-equality test (DOCS-01, D-01)"
    - "Each catalog row shows a tool name, a curated one-liner, and a link to that tool's /docs/[slug] HTTP page (DOCS-01, D-02, D-03)"
    - "The page includes 4 copy-pasteable connection snippets — Claude Desktop (mcp-remote bridge), Cursor (bare url), generic mcp.json (type:http), and the npx mcp-remote command — all referencing https://utils.api.orb3x.com/api/mcp (DOCS-02, D-05)"
    - "The nif_lookup AGT-portal 5–30 s latency is disclosed BOTH inline on its catalog row AND in a dedicated Performance & latency section (DOCS-03, D-07)"
    - "All 7 locale docs files carry a complete mcp DocsPage; chrome + 25 one-liners are localized while snippets/endpoint/tool names stay English; pnpm build (tsc) passes (DOCS-03, D-04)"
  artifacts:
    - path: "src/lib/mcp/catalog.ts"
      provides: "Canonical MCP_TOOL_CATALOG (25 {name, docsSlug}) + derived MCP_TOOL_NAMES — single source for page and test"
      contains: "MCP_TOOL_NAMES"
    - path: "src/lib/__tests__/mcp-catalog.test.ts"
      provides: "registry↔catalog set-equality + snippet/latency presence assertions"
      contains: "registerAllTools"
    - path: "src/lib/site-content.ts"
      provides: "'mcp' slug appended to docsPageSlugs + DocsTableCell union widening DocsTable.rows"
      contains: "DocsTableCell"
    - path: "src/components/site-primitives.tsx"
      provides: "DataTable renders object cells as next/link with key-collision fix"
      contains: "cell.href"
    - path: "src/locales/site/docs/en.ts"
      provides: "Authoritative mcp DocsPage (connect/catalog-*/latency sections)"
      contains: "mcp:"
  key_links:
    - from: "src/lib/__tests__/mcp-catalog.test.ts"
      to: "src/lib/mcp/registry.ts"
      via: "registerAllTools(mockServer) capture"
      pattern: "registerAllTools"
    - from: "src/lib/__tests__/mcp-catalog.test.ts"
      to: "src/lib/mcp/catalog.ts"
      via: "MCP_TOOL_NAMES set equality"
      pattern: "MCP_TOOL_NAMES"
    - from: "src/components/site-primitives.tsx"
      to: "src/lib/site-content.ts"
      via: "DocsTableCell import"
      pattern: "DocsTableCell"
    - from: "src/locales/site/docs/{pt,es,fr,de,zh,ja}.ts"
      to: "src/lib/site-content.ts"
      via: "DocsPageMap totality forces mcp entry"
      pattern: "mcp:"
---

<objective>
Deliver a single localized `/docs/mcp` documentation page for the public MCP server, end-to-end across all 7 site locales. The page documents the `POST /api/mcp` endpoint, the full 25-tool catalog (grouped by domain, each row linking to the existing per-domain `/docs/[slug]` HTTP page), four client connection snippets, and the NIF AGT-portal 5–30 s latency disclosure.

Purpose: Closes DOCS-01, DOCS-02, DOCS-03 — the final phase of the MCP milestone. Gives MCP clients (and humans) one canonical page to discover the endpoint, the tool catalog, and how to connect.
Output: A new `mcp` `DocsPage` in all 7 locale maps, a shared `catalog.ts` name source, a registry-coverage test, and a minimal backward-compatible `DocsTableCell` link-cell render extension.

This is a single vertical slice (MVP mode): one docs page delivered complete. No artificial horizontal layering — the catalog, snippets, and latency disclosure ship together as one coherent page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-documentation/05-CONTEXT.md
@.planning/phases/05-mcp-documentation/05-RESEARCH.md
@.planning/phases/05-mcp-documentation/05-PATTERNS.md
@.planning/phases/05-mcp-documentation/05-UI-SPEC.md

<interfaces>
<!-- Verified against source 2026-06-19. Use directly — no codebase exploration needed. -->

From src/lib/site-content.ts (current — Task 2 edits this):
```typescript
export const docsPageSlugs = [
  'getting-started','api-reference','validation','phone','address-geo',
  'calendar','finance','salary','time','documents','nif-verification',
  'translation','currency-exchange','examples',
] as const;                                   // APPEND 'mcp' as last entry (D-06)
export type DocsPageSlug = (typeof docsPageSlugs)[number];
export type DocsTable = { columns: string[]; rows: string[][]; };   // widen rows → DocsTableCell[][]
export type DocsCodeSample = { label: string; language: 'bash'|'json'|'js'|'ts'; content: string; };
export type DocsSection = { id: string; title: string; description?: string; paragraphs?: string[]; bullets?: string[]; table?: DocsTable; code?: DocsCodeSample; codes?: DocsCodeSample[]; note?: string; };
export type DocsPage = { slug: DocsPageSlug; label: string; description: string; eyebrow: string; title: string; intro: string; endpoint?: { method: 'GET'|'POST'; path: string; detail: string }; summaryCards: Array<{ label: string; value: string }>; sections: DocsSection[]; relatedSlugs: DocsPageSlug[]; };
export type DocsPageMap = Record<DocsPageSlug, DocsPage>;   // TOTAL — forces mcp key in all 7 locale files
```

From src/components/site-primitives.tsx (current DataTable — Task 2 edits this; `import Link from 'next/link'` already at line 4):
```typescript
export function DataTable({ columns, rows }: {
  columns: readonly string[];
  rows: readonly (readonly string[])[];        // widen → accept DocsTableCell union
}) { /* … */ }
// render today: <tr key={`${row[0]}-${index}`}> … <td key={cell}>{cell}</td>
// both `row[0]` (tr key) and `key={cell}` assume string — guard BOTH for object cells
```

From src/lib/mcp/registry.ts:
```typescript
export function registerAllTools(server): void;   // registers exactly 25 tools via server.registerTool(name, meta)
```

The 25 registered tool names (VERIFIED — RESEARCH lines 154-165):
health, salary_net, salary_gross, salary_employer_cost, phone_parse, phone_validate, phone_operator,
geo_provinces, geo_municipalities, geo_communes, address_normalize, address_suggest,
calendar_holidays, calendar_working_days, calendar_add_working_days,
finance_vat, finance_invoice_total, finance_inflation_adjust, currency_rates, currency_convert,
nif_lookup, translate_text, generate_invoice_pdf, generate_receipt_pdf, generate_contract_pdf
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create catalog single-source-of-truth + registry-coverage test (Wave 0 scaffold)</name>
  <files>src/lib/mcp/catalog.ts, src/lib/__tests__/mcp-catalog.test.ts</files>
  <read_first>
    - src/lib/mcp/registry.ts — confirm `registerAllTools` signature and that it calls `server.registerTool(name, meta)` for each tool
    - src/lib/__tests__/mcp-registry.test.ts — copy VERBATIM: the top-of-file `jest.mock('@/lib/agt-nif', …)` block AND the `mockServer.registerTool` capture scaffold; preserve import order (mock BEFORE importing the registry)
    - src/lib/site-content.ts (lines 1-18) — the `as const` → derived-type idiom; `DocsPageSlug` (imported here for slug typing); note 'mcp' is NOT yet in the slug union, so `docsSlug` values used in catalog.ts must be EXISTING slugs only
    - 05-PATTERNS.md (catalog.ts section, lines 26-87) — exact 25-entry `{name, docsSlug}` content + `as const satisfies` shape
    - 05-RESEARCH.md (lines 117-166) — Pattern 1 (single source), Pattern 2 (mock capture), Pattern 3 (two-way set equality), and the verified 25-name list
  </read_first>
  <behavior>
    - Test 1: `new Set(MCP_TOOL_NAMES)` deep-equals `new Set(registeredNames)` captured from `registerAllTools(mockServer)` — fails if ANY registered tool is missing from the catalog OR any catalog name is a phantom (D-01)
    - Test 2: `MCP_TOOL_NAMES.length` is exactly 25
    - Test 3 (defer assertions to Task 3 once the page exists — leave these as placeholders that read the en page): the en `mcp` page `connect` section has exactly 4 `codes`; each code content contains `https://utils.api.orb3x.com/api/mcp` OR `mcp-remote`
    - Test 4: the `nif_lookup` catalog row description contains `5–30` (EN DASH U+2013) AND the `latency` section `note` contains `5–30`
  </behavior>
  <action>
    Create `src/lib/mcp/catalog.ts` exporting `McpToolCatalogEntry = { name: string; docsSlug: DocsPageSlug | null }`, an ordered `MCP_TOOL_CATALOG` of the 25 `{name, docsSlug}` entries from 05-PATTERNS.md (salary→'salary', phone→'phone', geo_*/address_*→'address-geo', calendar→'calendar', finance→'finance', currency_*→'currency-exchange', nif_lookup→'nif-verification', translate_text→'translation', generate_*_pdf→'documents'), typed `as const satisfies readonly McpToolCatalogEntry[]`, plus `export const MCP_TOOL_NAMES = MCP_TOOL_CATALOG.map((t) => t.name)`. Import `DocsPageSlug` from `@/lib/site-content`. For the `health` tool's `docsSlug`, pick `'api-reference'` (it exists in `docsPageSlugs` today and is a valid platform-level target) — state this choice in the SUMMARY (health-slug discretion resolved to 'api-reference').

    Create `src/lib/__tests__/mcp-catalog.test.ts`: paste the `jest.mock('@/lib/agt-nif', …)` block verbatim from mcp-registry.test.ts (REQUIRED — cheerio ESM breaks jsdom without it), then import `registerAllTools` from `@/lib/mcp/registry` and `MCP_TOOL_NAMES` from `@/lib/mcp/catalog` AFTER the mock. Use the `mockServer` with a `registerTool(name, meta)` recorder, call `registerAllTools(mockServer as never)`, derive `registeredNames = Object.keys(registeredTools)`. Assert `new Set(MCP_TOOL_NAMES)` toEqual `new Set(registeredNames)` and length 25. Add the snippet/latency assertions reading the en page via the site-copy accessor used elsewhere (resolve the exact accessor by reading how en docs pages are consumed — e.g. `getSiteCopy('en').docsPages.mcp` or the equivalent in `src/lib/site-copy.ts`); if the page does not exist yet at this task, gate those 2 assertions behind a `describe` that Task 3 turns green, OR write them now knowing they go RED until Task 3 — RED is expected (TDD). Do NOT derive tool names from rendered copy.
  </action>
  <verify>
    <automated>pnpm test -- mcp-catalog 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/mcp/catalog.ts` exports `MCP_TOOL_NAMES` with exactly 25 names
    - `MCP_TOOL_CATALOG` is `as const satisfies readonly McpToolCatalogEntry[]` and imports `DocsPageSlug` from `@/lib/site-content`
    - `src/lib/__tests__/mcp-catalog.test.ts` retains the `jest.mock('@/lib/agt-nif', …)` block and imports it BEFORE the registry
    - The set-equality + length-25 assertions PASS (`registerAllTools` capture reconciles with `MCP_TOOL_NAMES`)
    - `pnpm test -- mcp-catalog` exits 0 for the coverage assertions (snippet/latency assertions may be RED until Task 3 — note this in the run)
  </acceptance_criteria>
  <done>Catalog name set proven equal to the live registry (25); test scaffold in place. Snippet/latency assertions authored (go green in Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Add 'mcp' slug + DocsTableCell link-cell extension (type + render)</name>
  <files>src/lib/site-content.ts, src/components/site-primitives.tsx</files>
  <read_first>
    - src/lib/site-content.ts (lines 1-69) — the `docsPageSlugs` array (append point), `DocsTable` type (line 26-29), and confirm `DocsPageMap` is total (line 69)
    - src/components/site-primitives.tsx (DataTable ~lines 417-456) — current props `rows: readonly (readonly string[])[]`, the `<tr key={`${row[0]}-${index}`}>` and `<td key={cell}>{cell}</td>` render; confirm `import Link from 'next/link'` exists at top
    - src/components/docs-detail-content.tsx (~line 175) — the relatedSlugs `<Link>` accent style `font-semibold text-primary` to mirror
    - 05-UI-SPEC.md (FLAGGED EXTENSION, lines 164-185) — Option A is mandated; the key-collision fix is REQUIRED
    - 05-PATTERNS.md (site-content.ts + site-primitives.tsx sections, lines 154-227) — exact edits incl. the guarded `<tr key>` and `<td key>`
  </read_first>
  <action>
    In `src/lib/site-content.ts`: (1) append `'mcp'` as the LAST entry of the `docsPageSlugs` `as const` array, after `'examples'` (D-06). (2) Add `export type DocsTableCell = string | { label: string; href: string };` and change `DocsTable.rows` from `string[][]` to `DocsTableCell[][]`. This is backward-compatible — existing `string[][]` literals still satisfy `DocsTableCell[][]`, so no existing locale row arrays need edits. Do NOT touch `DocsSection` or `DocsPage`.

    In `src/components/site-primitives.tsx`: import `DocsTableCell` from `@/lib/site-content`. Widen `DataTable`'s `rows` prop to `readonly (readonly DocsTableCell[])[]`. In the cell map, when `typeof cell === 'object'` render `<Link href={cell.href} className="font-semibold text-primary">{cell.label}</Link>` (mirroring docs-detail-content.tsx:175), else render the bare string as today. Fix BOTH key collisions: the `<tr key>` (currently reads `row[0]`) must guard for object first cells (`typeof row[0] === 'object' ? row[0].href : row[0]`), and the `<td key>` (currently `key={cell}`) must become `typeof cell === 'object' ? cell.href : `${cell}-${cellIndex}`` (add `cellIndex` to the map). No new CSS token, no new component. Keep `href` values internal/relative (`/docs/[slug]`) — never interpolate untrusted data (threat T-05-01).
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -15 && pnpm exec tsc --noEmit 2>&1 | grep -i "site-content\|site-primitives\|DocsTableCell" | head || echo "no type errors in edited files"</automated>
  </verify>
  <acceptance_criteria>
    - `docsPageSlugs` includes `'mcp'` as the final entry; `DocsPageSlug` now includes `'mcp'`
    - `DocsTableCell` union exported; `DocsTable.rows` is `DocsTableCell[][]`
    - `DataTable` renders object cells as a `next/link` styled `font-semibold text-primary`; string cells unchanged
    - No `key={cell}` remains for object cells — keys use `cell.href`/index; `<tr key>` guards object `row[0]`
    - Existing test suite stays green (`pnpm test` exits 0) — existing `string[][]` tables unaffected
  </acceptance_criteria>
  <done>'mcp' is a valid slug (generateStaticParams will pick it up); DataTable can render `/docs/[slug]` link cells without breaking existing string tables.</done>
</task>

<task type="auto">
  <name>Task 3: Author the mcp DocsPage in en.ts + all 6 locale overrides; pass build + full suite</name>
  <files>src/locales/site/docs/en.ts, src/locales/site/docs/pt.ts, src/locales/site/docs/es.ts, src/locales/site/docs/fr.ts, src/locales/site/docs/de.ts, src/locales/site/docs/zh.ts, src/locales/site/docs/ja.ts</files>
  <read_first>
    - src/locales/site/docs/en.ts — the `documents` entry (catalog table section) and `nif-verification` entry (endpoint block + `note`-bearing section) as the shape to mirror; confirm imports at top (`DocsPageMap, DocsSection`); find the `examples` entry / closing `}` of `enDocsPages` to insert `mcp:` after it
    - src/locales/site/docs/pt.ts (line 50, header) — confirm `export const ptDocsPages: DocsPageMap = {…}` total annotation (same for es/fr/de/zh/ja)
    - src/lib/site-copy.ts — `mergeDeep` fallback + `supportedLocales`; confirm how a docs page is read so Task 1's test accessor matches
    - src/lib/mcp/catalog.ts (from Task 1) — the 25 names + docsSlug per tool to build the catalog rows
    - 05-PATTERNS.md (en.ts section, lines 230-326) — the exact DocsPage envelope, the `connect` section with the 4 verified snippet bodies, the per-domain `catalog-*` table sections, and the `latency` section
    - 05-RESEARCH.md (lines 193-241) — the 4 verified snippet bodies (Claude Desktop = mcp-remote bridge NOT bare url; Cursor = bare url; generic = type:http + defensive comment; npx mcp-remote bash) and the security note (NO API key/Authorization header in any snippet)
    - 05-UI-SPEC.md (Section & Component Mapping, lines 122-160; Required content copy, lines 100-118) — section order (connect → catalog-* per group → latency), columns `['Tool','Description','Docs']`, 3 summaryCards, NIF copy
  </read_first>
  <action>
    In `src/locales/site/docs/en.ts`, author a complete `mcp: { … }` `DocsPage` entry on `enDocsPages` (insert after `examples`): slug `'mcp'`, `label` (e.g. 'MCP Server'), `description`, `eyebrow` (e.g. 'Model Context Protocol'), `title`, `intro` (25 Angola utility tools over one hosted MCP endpoint, Streamable HTTP), `endpoint: { method: 'POST', path: '/api/mcp', detail: 'Streamable HTTP; SSE disabled — use mcp-remote for SSE-only clients.' }`, three `summaryCards` (`{label:'Tools',value:'25 tools'}`, `{label:'Transport',value:'Streamable HTTP'}`, `{label:'Locales',value:'7 languages'}`), and `relatedSlugs` listing the 9 domain slugs (salary, phone, address-geo, calendar, finance, documents, nif-verification, translation, currency-exchange).

    Sections in order: (1) `connect` — inline section with `description` and `codes: [4]` exactly as the verified bodies in 05-RESEARCH lines 193-241: Claude Desktop json (mcp-remote bridge `command:'npx', args:['-y','mcp-remote','https://utils.api.orb3x.com/api/mcp']` + a note line that it can also be added via Settings → Connectors), Cursor json (bare `url`), Generic mcp.json (`type:'http'` + `url` + inline comment noting some clients use `streamable-http`/`transport`), and a bash `npx -y mcp-remote https://utils.api.orb3x.com/api/mcp`. Every snippet references `https://utils.api.orb3x.com/api/mcp`. NO API key/Authorization header in any snippet (security T-05-02 accept-by-design: public endpoint). (2) One `table` section per domain group (`catalog-salary`, `catalog-phone`, `catalog-address-geo` [combine geo_* + address_*], `catalog-calendar`, `catalog-finance`, `catalog-currency`, `catalog-nif`, `catalog-translation`, `catalog-documents`, `catalog-platform` [health]) — each with a curated group `description`, `columns: ['Tool','Description','Docs']`, and rows `[toolName, oneLiner, { label: <slug>, href: '/docs/<slug>' }]` for every tool in that group, using EXACTLY the 25 registry names so the Task 1 test stays green. For `nif_lookup` ONLY, append the inline latency note to its one-liner: `'Look up a NIF on the AGT portal. Live scrape — 5–30 s; set generous timeouts.'` (D-07a; use EN DASH U+2013). For `health`, link to `/docs/api-reference`. (3) `latency` — `{ id:'latency', title:'Performance & latency', description:…, note: 'AGT portal lookups take 5–30 s. Set generous client timeouts; the server already enforces a ~25 s upstream cap.' }` (D-07b, EN DASH).

    In each of the 6 override files (pt, es, fr, de, zh, ja), add a COMPLETE `mcp: DocsPage` entry (REQUIRED — `DocsPageMap` is total; partial = build failure). Localize: `label`, `eyebrow`, `title`, `intro`, `description`, each section `title`/`description`, `summaryCards` labels, the 25 tool one-liners (row col 2), and the `latency` note. Keep ENGLISH verbatim from en.ts: endpoint `path`, every snippet `codes[].content`, tool names (row col 1), the link cell `href`+`label`, and the `5–30` figure. Reuse en strings verbatim for the English-canonical fields. Do not edit any existing entry.
  </action>
  <verify>
    <automated>pnpm test -- mcp-catalog 2>&1 | tail -15 && pnpm build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `mcp` `DocsPage` present in ALL 7 locale `DocsPageMap`s (en + 6 overrides) — `pnpm build` (tsc totality) exits 0
    - en `mcp` page `connect` section has exactly 4 `codes`; each content contains `https://utils.api.orb3x.com/api/mcp` or `mcp-remote`
    - Claude Desktop snippet uses the `mcp-remote` bridge (`command:'npx'`, `args` with `mcp-remote`) — NOT a bare remote `url`
    - Catalog rows across all groups use exactly the 25 registry names → `pnpm test -- mcp-catalog` set-equality stays green
    - `nif_lookup` row one-liner contains `5–30` (EN DASH) AND the `latency` section `note` contains `5–30`
    - No snippet contains an API key, token, or `Authorization` header
    - `pnpm test -- mcp-catalog` exits 0 (all assertions incl. snippet/latency now green) AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The /docs/mcp page is fully authored and localized; build passes the 7-locale totality gate; the coverage + snippet + latency tests are all green. DOCS-01/02/03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored-content → rendered HTML | Catalog `href` and snippet strings are authored constants compiled into static pages — no user input crosses into this page. |
| docs page → MCP runtime | None this phase. The `/api/mcp` endpoint, its public-by-design auth posture, and rate limiting are owned by Phases 1–3 and are NOT modified here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering/Information Disclosure | `DocsTableCell.href` rendered via `next/link` | mitigate | `href` is an authored internal `/docs/[slug]` constant — never interpolated from untrusted input. Keep hrefs relative/internal; no open-redirect or XSS surface (Task 2 acceptance enforces relative hrefs). |
| T-05-02 | Information Disclosure | Connection snippets on the page | accept | Snippets contain ONLY the public endpoint URL — the API is public by design (REQUIREMENTS Out of Scope: no auth in v1). No API key, token, or `Authorization` header is embedded (Task 3 acceptance strips them). Accept: nothing secret to leak. |
| T-05-03 | Tampering (supply chain) | `mcp-remote` referenced in snippets | accept | `mcp-remote` is NOT added to `package.json` — it appears only inside documentation the END USER runs via `npx`. No project dependency added, so no supply-chain surface introduced by this phase. Package verified real (npm 0.1.38, RESEARCH). |

No threat is rated high. This is a static docs/content phase with one backward-compatible render extension; ASVS L1 surface is N/A (no user input, no new request handling).
</threat_model>

<verification>
Phase-level gates (run before `/gsd:verify-work`):
- `pnpm test -- mcp-catalog` — set equality (25), 4 snippets, NIF 5–30 s both surfaces all green
- `pnpm test` — full suite green (DocsTableCell widening did not break existing string tables)
- `pnpm build` — Next.js tsc passes the 7-locale `DocsPageMap` totality gate (the ONLY gate that catches a missing/partial `mcp` entry)

Manual (post-build, documented in VALIDATION.md): `pnpm dev` and visit the `/docs/mcp` route in each of the 7 locales; confirm catalog, 4 snippets, and the Performance & latency section render.
</verification>

<success_criteria>
- Navigating `/docs/mcp` in all 7 locales renders the `POST /api/mcp` endpoint + 25-tool catalog (criterion 1)
- The page has 4 copy-pasteable connection snippets (native-style Cursor/generic + `mcp-remote` bridge fallback) (criterion 2)
- The NIF 5–30 s AGT-portal latency is disclosed on the page with guidance (criterion 3)
- All 7 locale files follow the `mergeDeep` deep-merge pattern and compile (`pnpm build` exits 0) (criterion 4)
- The catalog tool-name set equals the live `registerAllTools` set (25) — coverage test green
</success_criteria>

<output>
Create `.planning/phases/05-mcp-documentation/05-01-SUMMARY.md` when done. Record: the resolved `health` docs-slug choice (`api-reference`), the exact site-copy accessor used by the test, the EN DASH (U+2013) usage in the `5–30` strings, and any locale-string decisions.
</output>
</content>
</invoke>

---
phase: 02-core-utility-tools
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/tools/geo.ts
  - src/lib/__tests__/mcp-tools-geo.test.ts
autonomous: true
requirements: [LOC-02]
tags: [mcp, geo, angola, tool-adapter]

must_haves:
  truths:
    - "Client calls geo_provinces and receives { country, countryName, provinces:[...] } with name/capital/municipalityCount per province"
    - "Client calls geo_municipalities (optional province filter) and receives the matching municipality list"
    - "Client calls geo_communes with a municipality (and optional province) and receives commune data"
    - "geo_municipalities with an unknown province returns { isError: true } code PROVINCE_NOT_FOUND"
    - "geo_communes with an ambiguous municipality returns { isError: true } code AMBIGUOUS_MUNICIPALITY"
  artifacts:
    - path: "src/lib/mcp/tools/geo.ts"
      provides: "registerGeoTools — registers geo_provinces, geo_municipalities, geo_communes"
      exports: ["registerGeoTools"]
      contains: "registerGeoTools"
    - path: "src/lib/__tests__/mcp-tools-geo.test.ts"
      provides: "Registration + happy-path + error-path coverage for LOC-02"
      contains: "registerGeoTools"
  key_links:
    - from: "src/lib/mcp/tools/geo.ts"
      to: "src/lib/angola/geo.ts"
      via: "named import of listAngolaProvinces/listAngolaMunicipalities/listAngolaCommunes"
      pattern: "from '@/lib/angola/geo'"
    - from: "src/lib/mcp/tools/geo.ts"
      to: "src/lib/mcp/tool-error.ts"
      via: "every callback wrapped in mcpToolHandler"
      pattern: "mcpToolHandler\\("
---

<objective>
Create the geo MCP tool module: `registerGeoTools(server)` registering `geo_provinces`, `geo_municipalities`, `geo_communes` as thin adapters over `src/lib/angola/geo.ts`, plus its Jest test. Wave-1 parallel domain plan (D-05) — creates ONLY its own files; does NOT touch `registry.ts`.

Purpose: Deliver LOC-02 — MCP clients can resolve Angola geolocation data (provinces, municipalities, communes).
Output: `src/lib/mcp/tools/geo.ts`, `src/lib/__tests__/mcp-tools-geo.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-utility-tools/02-CONTEXT.md
@.planning/phases/02-core-utility-tools/02-RESEARCH.md
@.planning/phases/02-core-utility-tools/02-PATTERNS.md

<interfaces>
From src/lib/mcp/tools/health.ts: registration template (z.object({}) empty schema for zero-arg tools).

From src/lib/angola/geo.ts (functions to wrap — do NOT modify):
```typescript
listAngolaProvinces()                                          // returns array of { name, slug, capital, municipalityCount }
listAngolaMunicipalities(provinceName?: string)                // throws RouteError('PROVINCE_NOT_FOUND', ..., 404) if filter not matched
listAngolaCommunes(municipalityName: string, provinceName?: string)
// throws: MISSING_QUERY_PARAMETER (empty mun), MUNICIPALITY_NOT_FOUND (404), AMBIGUOUS_MUNICIPALITY (400, needs province), PROVINCE_NOT_FOUND (404)
```

From app/geo/provinces/route.ts (HTTP envelope to MIRROR): { country: 'AO', countryName: 'Angola', provinces: listAngolaProvinces() }

From src/lib/mcp/tool-error.ts: mcpToolHandler — RouteError → isError. Never try/catch inside.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing test scaffold for geo tools</name>
  <files>src/lib/__tests__/mcp-tools-geo.test.ts</files>
  <read_first>
    - src/lib/__tests__/mcp-registry.test.ts (mock-server pattern)
    - src/lib/__tests__/mcp-tool-error.test.ts (error-path assertion)
    - .planning/phases/02-core-utility-tools/02-PATTERNS.md (§test template + adaptation table row "geo", lines 444-519)
  </read_first>
  <behavior>
    - registers geo_provinces, geo_municipalities, geo_communes
    - each tool has non-empty title and description (D-03)
    - geo_provinces happy path: {} → parsed JSON has a provinces array (non-empty) and country 'AO'
    - geo_municipalities happy path: { province: 'Luanda' } → returns a filtered list
    - geo_municipalities error path: { province: 'Unknown' } → isError:true, code PROVINCE_NOT_FOUND
    - geo_communes error path: { municipality: 'Calumbo' } → isError:true, code AMBIGUOUS_MUNICIPALITY
  </behavior>
  <action>
    Create the test using the mcp-registry.test.ts mock-server pattern. Import `registerGeoTools` from `@/lib/mcp/tools/geo`. Capture per-tool handlers (third registerTool arg) for invocation tests; `JSON.parse(result.content[0].text)`. Cover the behaviors above (use 'Calumbo' for the ambiguous case per RESEARCH Pitfall 3). Single quotes, 2-space, `@/` alias. MUST fail initially (RED).
  </action>
  <verify>
    <automated>pnpm test -- mcp-tools-geo 2>&1 | grep -qE "Cannot find module|registerGeoTools" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the test cases above; fails because geo.ts not yet implemented (RED).
  </acceptance_criteria>
  <done>Test file references registerGeoTools and fails pre-implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registerGeoTools (geo_provinces, geo_municipalities, geo_communes)</name>
  <files>src/lib/mcp/tools/geo.ts</files>
  <read_first>
    - src/lib/mcp/tools/health.ts (registration template + empty schema)
    - src/lib/mcp/tool-error.ts (mcpToolHandler)
    - src/lib/angola/geo.ts (functions to wrap; do NOT modify)
    - app/geo/provinces/route.ts (envelope to mirror), app/geo/municipalities/route.ts, app/geo/communes/route.ts (arg passing)
    - .planning/phases/02-core-utility-tools/02-RESEARCH.md (§GEO DOMAIN — schemas + 3 anti-collision descriptions + ambiguity gotcha, lines 316-387)
    - .planning/phases/02-core-utility-tools/02-PATTERNS.md (§geo.ts, lines 145-213)
  </read_first>
  <action>
    Create `src/lib/mcp/tools/geo.ts` exporting `registerGeoTools(server: McpServer): void` (D-04). Import `z`, `McpServer` type, `mcpToolHandler` from `../tool-error`, and `listAngolaProvinces, listAngolaMunicipalities, listAngolaCommunes` from `@/lib/angola/geo`. Register per D-01/D-02:
    - `geo_provinces` (title 'List Angola Provinces'): inputSchema `z.object({})`. Callback mirrors the HTTP envelope: `mcpToolHandler(async () => ({ country: 'AO', countryName: 'Angola', provinces: listAngolaProvinces() }))`.
    - `geo_municipalities` (title 'List Angola Municipalities'): inputSchema `z.object({ province: z.string().optional().describe(...) })`. Callback `({ province }) => listAngolaMunicipalities(province)`.
    - `geo_communes` (title 'List Angola Communes'): inputSchema `z.object({ municipality: z.string().min(1).describe(...), province: z.string().optional().describe(...) })`. Callback `({ municipality, province }) => listAngolaCommunes(municipality, province)`.
    Use describe() text and the three D-03 anti-collision descriptions verbatim from RESEARCH §GEO. The geo_communes description MUST mention the `province` disambiguation parameter (Pitfall 3). No try/catch inside callbacks — PROVINCE_NOT_FOUND / AMBIGUOUS_MUNICIPALITY propagate as RouteError. Single quotes, 2-space, named export.
  </action>
  <verify>
    <automated>pnpm test -- mcp-tools-geo && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tools/list (mock) includes geo_provinces, geo_municipalities, geo_communes
    - geo_provinces({}) → JSON has country 'AO' and a non-empty provinces array
    - geo_municipalities({ province: 'Unknown' }) → isError:true, code PROVINCE_NOT_FOUND
    - geo_communes({ municipality: 'Calumbo' }) → isError:true, code AMBIGUOUS_MUNICIPALITY
    - geo_communes description names the province disambiguation parameter
    - tsc clean
  </acceptance_criteria>
  <done>All geo tests pass (GREEN); three tools registered; provinces envelope mirrored; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → geo tool callback | Untrusted province/municipality strings cross into the adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | geo_communes municipality (empty/ambiguous/unknown) | mitigate | `z.string().min(1)` on municipality; domain function throws MISSING_QUERY_PARAMETER/MUNICIPALITY_NOT_FOUND/AMBIGUOUS_MUNICIPALITY/PROVINCE_NOT_FOUND → mcpToolHandler maps to isError |
| T-02-06 | Information Disclosure | static geo data listing | accept | Public reference data (provinces/municipalities/communes); no PII, no secrets |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `pnpm test -- mcp-tools-geo` green
- `pnpm exec tsc --noEmit` clean
- registry.ts untouched (disjoint files_modified per D-05)
</verification>

<success_criteria>
LOC-02 satisfied: geo_provinces/municipalities/communes registered; provinces returns the HTTP-mirroring envelope; unknown province and ambiguous municipality return structured isError.
</success_criteria>

<output>
Create `.planning/phases/02-core-utility-tools/02-03-SUMMARY.md` when done.
</output>

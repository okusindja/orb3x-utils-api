---
phase: 02-core-utility-tools
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/tools/calendar.ts
  - src/lib/__tests__/mcp-tools-calendar.test.ts
autonomous: true
requirements: [CAL-01]
tags: [mcp, calendar, holidays, angola, tool-adapter]

must_haves:
  truths:
    - "Client calls calendar_holidays with a year (or none) and receives { year, holidays:[...], assumptions } — current year when omitted"
    - "Client calls calendar_working_days with from/to ISO dates and receives the working-day count excluding weekends + holidays"
    - "Client calls calendar_add_working_days with date + days and receives the offset result date; days:0 returns direction:'none'"
    - "calendar_holidays with an out-of-range year returns { isError: true } code INVALID_YEAR"
  artifacts:
    - path: "src/lib/mcp/tools/calendar.ts"
      provides: "registerCalendarTools — registers calendar_holidays, calendar_working_days, calendar_add_working_days"
      exports: ["registerCalendarTools"]
      contains: "registerCalendarTools"
    - path: "src/lib/__tests__/mcp-tools-calendar.test.ts"
      provides: "Registration + happy-path + error-path coverage for CAL-01"
      contains: "registerCalendarTools"
  key_links:
    - from: "src/lib/mcp/tools/calendar.ts"
      to: "src/lib/angola/calendar.ts"
      via: "named import of listAngolanHolidays/calculateWorkingDays/addWorkingDays"
      pattern: "from '@/lib/angola/calendar'"
    - from: "src/lib/mcp/tools/calendar.ts"
      to: "src/lib/mcp/tool-error.ts"
      via: "every callback wrapped in mcpToolHandler"
      pattern: "mcpToolHandler\\("
---

<objective>
Create the calendar MCP tool module: `registerCalendarTools(server)` registering `calendar_holidays`, `calendar_working_days`, `calendar_add_working_days` as thin adapters over `src/lib/angola/calendar.ts`, plus its Jest test. Wave-1 parallel domain plan (D-05) — creates ONLY its own files; does NOT touch `registry.ts`.

Purpose: Deliver CAL-01 — MCP clients can list Angola holidays, count working days, and offset by working days.
Output: `src/lib/mcp/tools/calendar.ts`, `src/lib/__tests__/mcp-tools-calendar.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-utility-tools/02-CONTEXT.md
@.planning/phases/02-core-utility-tools/02-RESEARCH.md
@.planning/phases/02-core-utility-tools/02-PATTERNS.md

<interfaces>
From src/lib/mcp/tools/health.ts: registration template.

From src/lib/angola/calendar.ts (functions to wrap — do NOT modify):
```typescript
listAngolanHolidays(year: number)               // returns 13 holidays (10 fixed + Carnival Mon/Tue + Good Friday); throws RouteError('INVALID_YEAR', ..., 400) outside 2000-2100
calculateWorkingDays({ from: string; to: string })  // throws MISSING_QUERY_PARAMETER, INVALID_DATE, INVALID_DATE_RANGE, INVALID_YEAR
addWorkingDays({ date: string; days: number })   // days:0 returns { ..., direction: 'none' } early; throws MISSING_QUERY_PARAMETER, INVALID_DATE, INVALID_INTEGER
```

From app/calendar/holidays/route.ts (envelope + runtime year default to MIRROR):
{ year: resolvedYear, holidays: listAngolanHolidays(resolvedYear), assumptions: [...] } where resolvedYear = year ?? new Date().getUTCFullYear()

From src/lib/mcp/tool-error.ts: mcpToolHandler — RouteError → isError. Never try/catch inside.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing test scaffold for calendar tools</name>
  <files>src/lib/__tests__/mcp-tools-calendar.test.ts</files>
  <read_first>
    - src/lib/__tests__/mcp-registry.test.ts (mock-server pattern)
    - src/lib/__tests__/mcp-tool-error.test.ts (error-path assertion)
    - .planning/phases/02-core-utility-tools/02-PATTERNS.md (§test template + adaptation table row "calendar", lines 444-519)
  </read_first>
  <behavior>
    - registers calendar_holidays, calendar_working_days, calendar_add_working_days
    - each tool has non-empty title and description (D-03)
    - calendar_holidays happy path: { year: 2026 } → parsed JSON holidays array length === 13, year === 2026
    - calendar_add_working_days: { date: '2026-01-01', days: 0 } → parsed JSON direction === 'none'
    - calendar_holidays error path: { year: 1990 } → isError:true, code INVALID_YEAR
  </behavior>
  <action>
    Create the test using the mcp-registry.test.ts mock-server pattern. Import `registerCalendarTools` from `@/lib/mcp/tools/calendar`. Capture per-tool handlers (third registerTool arg); `JSON.parse(result.content[0].text)`. Cover the behaviors above. Single quotes, 2-space, `@/` alias. MUST fail initially (RED).
  </action>
  <verify>
    <automated>pnpm test -- mcp-tools-calendar 2>&1 | grep -qE "Cannot find module|registerCalendarTools" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the test cases above; fails because calendar.ts not yet implemented (RED).
  </acceptance_criteria>
  <done>Test file references registerCalendarTools and fails pre-implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registerCalendarTools (holidays, working_days, add_working_days)</name>
  <files>src/lib/mcp/tools/calendar.ts</files>
  <read_first>
    - src/lib/mcp/tools/health.ts (registration template)
    - src/lib/mcp/tool-error.ts (mcpToolHandler)
    - src/lib/angola/calendar.ts (functions to wrap; do NOT modify)
    - app/calendar/holidays/route.ts (envelope + runtime year default), app/calendar/working-days/route.ts, app/calendar/add-working-days/route.ts
    - .planning/phases/02-core-utility-tools/02-RESEARCH.md (§CALENDAR DOMAIN — schemas + 3 anti-collision descriptions + runtime-year-default note, lines 455-550)
    - .planning/phases/02-core-utility-tools/02-PATTERNS.md (§calendar.ts, lines 277-358)
  </read_first>
  <action>
    Create `src/lib/mcp/tools/calendar.ts` exporting `registerCalendarTools(server: McpServer): void` (D-04). Import `z`, `McpServer` type, `mcpToolHandler` from `../tool-error`, and `listAngolanHolidays, calculateWorkingDays, addWorkingDays` from `@/lib/angola/calendar`. Register per D-01/D-02:
    - `calendar_holidays` (title 'Angola Public Holidays'): inputSchema `z.object({ year: z.number().int().min(2000).max(2100).optional().describe(...) })`. Callback resolves year at runtime: `const resolvedYear = year ?? new Date().getUTCFullYear();` then returns `{ year: resolvedYear, holidays: listAngolanHolidays(resolvedYear), assumptions: [...] }` mirroring the HTTP envelope. CRITICAL: do NOT use a Zod `.default()` for year (Pitfall 2 — would freeze the year at module load); resolve inside the callback. Copy the two assumptions strings verbatim from RESEARCH §calendar_holidays.
    - `calendar_working_days` (title 'Angola Working Days Count'): inputSchema `z.object({ from: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).describe(...), to: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).describe(...) })`. Callback `({ from, to }) => calculateWorkingDays({ from, to })`.
    - `calendar_add_working_days` (title 'Angola Add Working Days'): inputSchema `z.object({ date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).describe(...), days: z.number().int().describe(...) })`. days has NO .nonnegative() — negatives go backward. Callback `({ date, days }) => addWorkingDays({ date, days })`.
    Use describe() text and the three D-03 anti-collision descriptions verbatim from RESEARCH §CALENDAR (each names its siblings). No try/catch inside callbacks — INVALID_YEAR/INVALID_DATE/etc propagate as RouteError. Single quotes, 2-space, named export.
  </action>
  <verify>
    <automated>pnpm test -- mcp-tools-calendar && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tools/list (mock) includes calendar_holidays, calendar_working_days, calendar_add_working_days
    - calendar_holidays({ year: 2026 }) → JSON holidays array length 13, year 2026
    - calendar_add_working_days({ date: '2026-01-01', days: 0 }) → direction 'none'
    - calendar_holidays({ year: 1990 }) → isError:true, code INVALID_YEAR
    - year resolved via `year ?? new Date().getUTCFullYear()` in callback (no Zod .default for year)
    - tsc clean
  </acceptance_criteria>
  <done>All calendar tests pass (GREEN); three tools registered; runtime year default; holidays envelope mirrored; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → calendar tool callback | Untrusted year/date/days arguments cross into the adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | calendar inputs (out-of-range year, malformed/inverted date range, non-int days) | mitigate | Zod `.int().min(2000).max(2100)`, ISO-date regex, `.int()` on days; domain functions throw INVALID_YEAR/INVALID_DATE/INVALID_DATE_RANGE/INVALID_INTEGER → mcpToolHandler maps to isError |
| T-02-10 | Denial of Service | calendar_working_days spanning a huge date range (per-year holiday build) | accept | Range bounded by 2000-2100 holiday lookup which throws INVALID_YEAR outside range; computation is O(days), pure in-memory |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `pnpm test -- mcp-tools-calendar` green
- `pnpm exec tsc --noEmit` clean
- registry.ts untouched (disjoint files_modified per D-05)
</verification>

<success_criteria>
CAL-01 satisfied: calendar_holidays/working_days/add_working_days registered; holidays returns the mirroring envelope with runtime year default; days:0 returns direction 'none'; out-of-range year returns structured isError.
</success_criteria>

<output>
Create `.planning/phases/02-core-utility-tools/02-05-SUMMARY.md` when done.
</output>

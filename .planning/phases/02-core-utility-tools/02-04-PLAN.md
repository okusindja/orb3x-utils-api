---
phase: 02-core-utility-tools
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/tools/address.ts
  - src/lib/__tests__/mcp-tools-address.test.ts
autonomous: true
requirements: [LOC-03]
tags: [mcp, address, angola, tool-adapter]

must_haves:
  truths:
    - "Client calls address_normalize with a raw address string and receives { input, normalized, components, diagnostics }"
    - "Client calls address_suggest with a query and receives an array of matching suggestions (up to limit)"
    - "address_normalize does NOT throw on unresolved components — it returns null components, not isError"
    - "address_normalize with empty string returns { isError: true } code INVALID_ADDRESS (guarded by z.string().min(1))"
  artifacts:
    - path: "src/lib/mcp/tools/address.ts"
      provides: "registerAddressTools — registers address_normalize, address_suggest"
      exports: ["registerAddressTools"]
      contains: "registerAddressTools"
    - path: "src/lib/__tests__/mcp-tools-address.test.ts"
      provides: "Registration + happy-path coverage for LOC-03"
      contains: "registerAddressTools"
  key_links:
    - from: "src/lib/mcp/tools/address.ts"
      to: "src/lib/angola/address.ts"
      via: "named import of normalizeAngolanAddress/suggestAngolanAddressParts"
      pattern: "from '@/lib/angola/address'"
    - from: "src/lib/mcp/tools/address.ts"
      to: "src/lib/mcp/tool-error.ts"
      via: "every callback wrapped in mcpToolHandler"
      pattern: "mcpToolHandler\\("
---

<objective>
Create the address MCP tool module: `registerAddressTools(server)` registering `address_normalize` and `address_suggest` as thin adapters over `src/lib/angola/address.ts`, plus its Jest test. Wave-1 parallel domain plan (D-05) — creates ONLY its own files; does NOT touch `registry.ts`.

Purpose: Deliver LOC-03 — MCP clients can normalize an Angola address and autocomplete address parts.
Output: `src/lib/mcp/tools/address.ts`, `src/lib/__tests__/mcp-tools-address.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-utility-tools/02-CONTEXT.md
@.planning/phases/02-core-utility-tools/02-RESEARCH.md
@.planning/phases/02-core-utility-tools/02-PATTERNS.md

<interfaces>
From src/lib/mcp/tools/health.ts: registration template.

From src/lib/angola/address.ts (functions to wrap — do NOT modify):
```typescript
normalizeAngolanAddress(rawAddress: string)
// returns { input, normalized, components, diagnostics }; throws RouteError('INVALID_ADDRESS', ..., 400) on empty.
// best-effort: unresolved components are null with diagnostics.* false — NOT an error.
suggestAngolanAddressParts({ query: string; type?: 'province'|'municipality'|'commune'|'bairro'; province?: string; municipality?: string; limit?: number })
// returns suggestion array; throws RouteError('MISSING_QUERY_PARAMETER', ..., 400) on empty query. Own default limit = 8.
```

From src/lib/mcp/tool-error.ts: mcpToolHandler — RouteError → isError. Never try/catch inside.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing test scaffold for address tools</name>
  <files>src/lib/__tests__/mcp-tools-address.test.ts</files>
  <read_first>
    - src/lib/__tests__/mcp-registry.test.ts (mock-server pattern)
    - src/lib/__tests__/mcp-tool-error.test.ts (error-path assertion)
    - .planning/phases/02-core-utility-tools/02-PATTERNS.md (§test template + adaptation table row "address", lines 444-519)
  </read_first>
  <behavior>
    - registers address_normalize, address_suggest
    - each tool has non-empty title and description (D-03)
    - address_normalize happy path: { address: 'av. lenine, ingombota, luanda' } → parsed JSON has a components field defined
    - address_suggest happy path: { query: 'lua' } → parsed JSON is/has an array of suggestions
  </behavior>
  <action>
    Create the test using the mcp-registry.test.ts mock-server pattern. Import `registerAddressTools` from `@/lib/mcp/tools/address`. Capture per-tool handlers (third registerTool arg); `JSON.parse(result.content[0].text)`. Cover the behaviors above. Note empty-query/empty-address are blocked by Zod min(1) before the handler, so assert registration + happy paths here. Single quotes, 2-space, `@/` alias. MUST fail initially (RED).
  </action>
  <verify>
    <automated>pnpm test -- mcp-tools-address 2>&1 | grep -qE "Cannot find module|registerAddressTools" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the test cases above; fails because address.ts not yet implemented (RED).
  </acceptance_criteria>
  <done>Test file references registerAddressTools and fails pre-implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registerAddressTools (address_normalize, address_suggest)</name>
  <files>src/lib/mcp/tools/address.ts</files>
  <read_first>
    - src/lib/mcp/tools/health.ts (registration template)
    - src/lib/mcp/tool-error.ts (mcpToolHandler)
    - src/lib/angola/address.ts (functions to wrap; do NOT modify; note KNOWN_BAIRROS curated set)
    - app/address/normalize/route.ts, app/address/suggest/route.ts (HTTP q→query mapping, limit integer guard)
    - .planning/phases/02-core-utility-tools/02-RESEARCH.md (§ADDRESS DOMAIN — schemas + 2 anti-collision descriptions + fuzzy-behavior note, lines 389-451)
    - .planning/phases/02-core-utility-tools/02-PATTERNS.md (§address.ts, lines 216-273)
  </read_first>
  <action>
    Create `src/lib/mcp/tools/address.ts` exporting `registerAddressTools(server: McpServer): void` (D-04). Import `z`, `McpServer` type, `mcpToolHandler` from `../tool-error`, and `normalizeAngolanAddress, suggestAngolanAddressParts` from `@/lib/angola/address`. Register per D-01/D-02:
    - `address_normalize` (title 'Normalize Angola Address'): inputSchema `z.object({ address: z.string().min(1).describe(...) })`. Callback `({ address }) => normalizeAngolanAddress(address)`.
    - `address_suggest` (title 'Suggest Angola Address Parts'): inputSchema `z.object({ query: z.string().min(1).describe(...), type: z.enum(['province','municipality','commune','bairro']).optional().describe(...), province: z.string().optional().describe(...), municipality: z.string().optional().describe(...), limit: z.number().int().positive().optional().default(8).describe(...) })`. Callback `({ query, type, province, municipality, limit }) => suggestAngolanAddressParts({ query, type, province, municipality, limit })`.
    Use describe() text and the two D-03 anti-collision descriptions verbatim from RESEARCH §ADDRESS (each names the sibling). The address_normalize description MUST note best-effort resolution (null components, not error) per the fuzzy-behavior note. Use `z.number()` not `z.coerce` for limit (Pitfall 1/5 — trust the Zod int). No try/catch inside callbacks. Single quotes, 2-space, named export.
  </action>
  <verify>
    <automated>pnpm test -- mcp-tools-address && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tools/list (mock) includes address_normalize, address_suggest
    - address_normalize({ address: 'av. lenine, ingombota, luanda' }) → JSON has components field
    - address_suggest({ query: 'lua' }) → suggestion array returned
    - address_normalize description documents best-effort (null components) behavior
    - tsc clean
  </acceptance_criteria>
  <done>All address tests pass (GREEN); two tools registered; fuzzy behavior documented; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → address tool callback | Untrusted address/query strings and limit cross into the adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | address_normalize/suggest empty string inputs | mitigate | `z.string().min(1)` on address/query; domain functions throw INVALID_ADDRESS/MISSING_QUERY_PARAMETER → mcpToolHandler maps to isError |
| T-02-08 | Denial of Service | oversized `limit` in address_suggest | mitigate | `z.number().int().positive()`; function returns a deduped slice over in-memory data — no DB query, bounded work |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `pnpm test -- mcp-tools-address` green
- `pnpm exec tsc --noEmit` clean
- registry.ts untouched (disjoint files_modified per D-05)
</verification>

<success_criteria>
LOC-03 satisfied: address_normalize and address_suggest registered; normalize returns structured components (best-effort, null on unresolved); suggest returns a bounded suggestion array.
</success_criteria>

<output>
Create `.planning/phases/02-core-utility-tools/02-04-SUMMARY.md` when done.
</output>

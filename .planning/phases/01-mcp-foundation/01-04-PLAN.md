---
phase: 01-mcp-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/angola/bank-images.ts
  - src/lib/angola/banks.ts
  - src/lib/__tests__/angola-banks.test.ts
autonomous: true
requirements: [PERF-01]
must_haves:
  truths:
    - "bank-images.ts no longer contains inline base64; logos are read from public/bank-logos/ by path"
    - "getAngolaBankLogoBytes returns Uint8Array for present logos and null for missing ones without throwing (D-06)"
    - "BANK_IMAGE_CODE_ALIASES (BPT->POSTAL, BSOL->SOL, BVB->BV, FNB->FINIBANCO) still drives lookup"
  artifacts:
    - path: "src/lib/angola/bank-images.ts"
      provides: "getAngolaBankLogoPath path resolver (no base64)"
      exports: ["getAngolaBankLogoPath"]
    - path: "src/lib/angola/banks.ts"
      provides: "getAngolaBankLogoBytes filesystem reader"
      exports: ["getAngolaBankLogoBytes"]
  key_links:
    - from: "src/lib/angola/banks.ts"
      to: "src/lib/angola/bank-images.ts"
      via: "getAngolaBankLogoPath(imageKey) then readFileSync"
      pattern: "getAngolaBankLogoPath"
    - from: "src/lib/angola/bank-images.ts"
      to: "public/bank-logos/"
      via: "join(process.cwd(), 'public', 'bank-logos') + existsSync"
      pattern: "bank-logos"
---

<objective>
Refactor `bank-images.ts` from ~1.4 MB inline base64 to filesystem path references against the
PNG files extracted in plan 01, and update its sole consumer `banks.ts` to read logo bytes from
disk with graceful degradation. This eliminates the cold-start / bundle penalty and the future
PDF-payload ceiling risk (PERF-01) before any document tools (Phase 4).

Purpose: PERF-01 — remove the inline base64 weight; future-proof Phase 4 PDF tools.
Output: refactored bank-images.ts + banks.ts, and updated bank tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mcp-foundation/01-RESEARCH.md
@.planning/phases/01-mcp-foundation/01-PATTERNS.md

<interfaces>
<!-- Current banks.ts symbols to remove/replace. Extracted from codebase. -->
banks.ts line 9:   import { ANGOLA_BANK_IMAGE_DATA } from '@/lib/angola/bank-images'  (REMOVE)
banks.ts line 65:  const BANK_IMAGE_PLACEHOLDER = ANGOLA_BANK_IMAGE_DATA.PLACEHOLDER  (REMOVE)
banks.ts line 66:  const bankImageCache = new Map<string,string>()                    (REMOVE)
banks.ts ~272-283: private getAngolaBankImage(bank) using ANGOLA_BANK_IMAGE_DATA      (REPLACE)
banks.ts lines 71, 167, 226: callers `image: getAngolaBankImage(bank)`                (UPDATE)
banks.ts ~58-63:   BANK_IMAGE_CODE_ALIASES { BPT:'POSTAL', BSOL:'SOL', BVB:'BV', FNB:'FINIBANCO' } (KEEP)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace bank-images.ts base64 with filesystem path resolver</name>
  <files>src/lib/angola/bank-images.ts</files>
  <read_first>
    - src/lib/angola/bank-images.ts (current base64 module — fully replaced)
    - src/lib/__tests__/bank-images.test.ts (RED scaffold from plan 01 — make it GREEN)
    - .planning/phases/01-mcp-foundation/01-PATTERNS.md (bank-images.ts after-refactor shape; node: import style)
    - .planning/phases/01-mcp-foundation/01-RESEARCH.md (Pattern 5; Pitfall 6 process.cwd standalone)
  </read_first>
  <behavior>
    - getAngolaBankLogoPath('BAI') returns a path ending in `BAI.png` (file exists from plan 01).
    - getAngolaBankLogoPath('NONEXISTENT') returns null.
    - Input is uppercased before lookup so lowercase codes resolve.
  </behavior>
  <action>
    Replace the entire contents of `src/lib/angola/bank-images.ts`. Remove the
    `ANGOLA_BANK_IMAGE_DATA` export entirely (and all base64 strings). Import `existsSync` from
    `node:fs` and `join` from `node:path`. Define
    `const LOGOS_DIR = join(process.cwd(), 'public', 'bank-logos')` (Pitfall 6: works in Vercel
    standalone because `public/` is copied into the standalone bundle at the project root). Export
    `getAngolaBankLogoPath(code: string): string | null` that uppercases `code`, builds
    `join(LOGOS_DIR, `${code}.png`)`, and returns the path if `existsSync` is true else `null`.
    No fenced code, no base64, no module-level mutable state.
  </action>
  <verify>
    <automated>pnpm test -- bank-images && node -e "const s=require('fs').readFileSync('src/lib/angola/bank-images.ts','utf8'); if(s.includes('ANGOLA_BANK_IMAGE_DATA')||/base64,/.test(s)){console.error('base64 residue');process.exit(1);}"</automated>
  </verify>
  <acceptance_criteria>
    - `bank-images.test.ts` passes: path-ends-with-BAI.png and null-for-missing
    - The file contains no `ANGOLA_BANK_IMAGE_DATA` export and no `base64,` substring
    - Uses `node:fs` / `node:path` imports and the `@/`-free relative-free style of the module
  </acceptance_criteria>
  <done>bank-images.ts is a pure path resolver; base64 removed; PERF-01 path test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update banks.ts to read logo bytes from disk and fix tests</name>
  <files>src/lib/angola/banks.ts, src/lib/__tests__/angola-banks.test.ts</files>
  <read_first>
    - src/lib/angola/banks.ts (lines 9, 58-66, 71, 167, 226, 272-283 per interfaces block)
    - src/lib/angola/bank-images.ts (just refactored — getAngolaBankLogoPath)
    - src/lib/__tests__/angola-banks.test.ts lines 27-55 (base64 assertions that WILL break)
    - .planning/phases/01-mcp-foundation/01-PATTERNS.md (banks.ts self-modification section)
  </read_first>
  <behavior>
    - getAngolaBankLogoBytes(bank) returns a Uint8Array when the (aliased) logo file exists.
    - getAngolaBankLogoBytes(bank) returns null when the file is missing, without throwing (D-06).
    - Alias resolution: bank.code BSOL resolves via SOL.png, BPT via POSTAL.png, etc.
  </behavior>
  <action>
    In `banks.ts`: remove the `ANGOLA_BANK_IMAGE_DATA` import (line 9), the
    `BANK_IMAGE_PLACEHOLDER` const (line 65), and the `bankImageCache` Map (line 66). Keep
    `BANK_IMAGE_CODE_ALIASES` unchanged. Add imports `readFileSync` from `node:fs` and
    `getAngolaBankLogoPath` from `@/lib/angola/bank-images`. Replace the private
    `getAngolaBankImage` function with an exported `getAngolaBankLogoBytes(bank: AngolaBank):
    Uint8Array | null` that computes `imageKey = BANK_IMAGE_CODE_ALIASES[bank.code] ?? bank.code`,
    calls `getAngolaBankLogoPath(imageKey)`, returns `null` if no path, else
    `try { return readFileSync(path) } catch { return null }` (D-06 graceful degradation). Update
    all three callers (`getAngolaBanks` ~line 71, `validateAngolanBankAccount` ~line 167,
    `validateAngolanIban` ~line 226): change `image: getAngolaBankImage(bank)` to
    `image: getAngolaBankLogoBytes(bank)`. Then update `angola-banks.test.ts` lines 27-55: the
    four assertions matching `data:image/...;base64,` patterns must be rewritten to assert the new
    return contract — `getAngolaBankLogoBytes` returns a `Uint8Array` (instanceof / length > 0) for
    a known bank and `null` for a bank whose logo file is absent. Keep all non-image assertions
    intact. Do this atomically so no `ANGOLA_BANK_IMAGE_DATA` reference survives (Pitfall 3).
  </action>
  <verify>
    <automated>pnpm test -- angola-banks && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `angola-banks.test.ts` passes with the rewritten image assertions (Uint8Array / null)
    - No `ANGOLA_BANK_IMAGE_DATA`, `BANK_IMAGE_PLACEHOLDER`, or `bankImageCache` reference remains in `banks.ts`
    - `getAngolaBankLogoBytes` is exported and returns `null` for a missing file without throwing
    - `tsc --noEmit` passes
  </acceptance_criteria>
  <done>banks.ts reads logo bytes from disk with graceful skip; tests updated; PERF-01 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> server | Logo bytes read at request time from public/bank-logos/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-FS | Tampering | getAngolaBankLogoPath lookup | accept | `code` is uppercased and concatenated to a fixed `.png` suffix under a fixed LOGOS_DIR; bank codes originate from the in-repo BANKS table, not user input — no path traversal surface in Phase 1 |
| T-01-AVL | Denial of Service | missing/corrupt logo file | mitigate | getAngolaBankLogoBytes returns null on missing/unreadable file (D-06); PDF/bank metadata generation proceeds without the logo rather than failing |
</threat_model>

<verification>
- `pnpm test -- bank-images angola-banks` GREEN.
- `pnpm exec tsc --noEmit` passes.
- grep confirms no `ANGOLA_BANK_IMAGE_DATA` / `base64,` residue in bank-images.ts or banks.ts.
</verification>

<success_criteria>
- bank-images.ts contains no inline base64; exposes getAngolaBankLogoPath only (PERF-01).
- banks.ts reads logo bytes from public/bank-logos/ via getAngolaBankLogoBytes, null on missing (D-06).
- Alias mapping preserved; existing bank tests updated and green.
</success_criteria>

<output>
Create `.planning/phases/01-mcp-foundation/01-04-SUMMARY.md` when done.
Note whether any non-PNG-origin logo bytes (flagged in plan 01) embed/read correctly, for Phase 4.
</output>

---
phase: 01-mcp-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified: []
autonomous: false
requirements: [MCP-01, MCP-02, MCP-03, MCP-05]
must_haves:
  truths:
    - "An MCP client connects to /api/mcp over Streamable HTTP AND SSE and gets valid JSON-RPC, verified via MCP Inspector and a real MCP client (D-09)"
    - "tools/list shows the health tool; calling health returns ok status"
    - "Exceeding the throttle limit yields a 429"
    - "The full Jest suite is green"
  artifacts: []
  key_links:
    - from: "MCP Inspector"
      to: "/api/mcp"
      via: "Streamable HTTP + SSE connect"
      pattern: "connect"
---

<objective>
Close Phase 1 with the D-09 verification gate: run the full test suite, then verify the live
endpoint with MCP Inspector locally and with a real MCP client against the deployed `/api/mcp`.
This is the human-in-the-loop confirmation that the foundation actually works end-to-end before
any domain tools are built.

Purpose: D-09 — Inspector + real-client verification is the phase's definition of done.
Output: a verified, reachable, rate-limited MCP endpoint serving the health tool.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mcp-foundation/01-RESEARCH.md
@.planning/phases/01-mcp-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full suite and type-check; start dev server</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/01-mcp-foundation/01-VALIDATION.md (phase gate: full suite green)
    - .planning/phases/01-mcp-foundation/01-RESEARCH.md (Verification Specifics D-09)
  </read_first>
  <action>
    Run `pnpm test` (full suite) and `pnpm exec tsc --noEmit` and confirm both are green —
    this is the phase gate before the human checkpoint. Then start the dev server with
    `pnpm dev` (background) so the checkpoint task can connect MCP Inspector at
    `http://localhost:3000/api/mcp`. If the SSE transport path observed in Inspector differs
    from `/api/sse`, record it so the plan 03 `middleware.ts` matcher can be corrected.
  </action>
  <verify>
    <automated>pnpm test && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (all existing + new suites green)
    - `pnpm exec tsc --noEmit` exits 0
    - Dev server reachable at http://localhost:3000
  </acceptance_criteria>
  <done>Full suite + type-check green; dev server up for Inspector.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-09 live verification (MCP Inspector + real client + rate limit)</name>
  <action>Human-verify checkpoint — no autonomous code change. The executor presents the verification steps below and pauses for the developer to run MCP Inspector and a real MCP client against the endpoint, then resume.</action>
  <what-built>
    Live MCP endpoint at /api/mcp serving the health stub tool, behind a per-IP rate limiter.
    Automated: full Jest suite and tsc are green; dev server running.
  </what-built>
  <how-to-verify>
    1. In a new terminal run `npx @modelcontextprotocol/inspector@0.22.0` (opens http://localhost:5173).
    2. Set Transport = Streamable HTTP, URL = http://localhost:3000/api/mcp, click Connect — connect must succeed (no 404 / transport error). This confirms MCP-01/02.
    3. Open the Tools tab — the `health` tool must appear with its title and description. This confirms MCP-03.
    4. Call `health` — the response must be `{ status: 'ok', server: 'orb3x-utils-mcp', timestamp: '...' }`.
    5. Switch Transport = SSE, reconnect — verify it connects (D-08 both transports). Note the actual SSE GET path in the network tab; if it is not `/api/sse`, report it so the middleware matcher is fixed.
    6. Rate limit: from the browser console (or Inspector repeat-call) send 61+ requests within a minute to http://localhost:3000/api/mcp — the 61st must return 429 with a Retry-After header. This confirms MCP-05.
    7. Deployed real client (after `vercel` deploy): add the orb3x-utils server to Claude Desktop/Code config pointing at https://<deployment>/api/mcp and confirm the `health` tool is listed and callable. This confirms MCP-01 against a real client.
  </how-to-verify>
  <resume-signal>Type "approved" once Inspector connects on both transports, health returns ok, 429 fires past the limit, and a real client lists health; or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> deployed /api/mcp | First real exposure of the unauthenticated endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-VERIFY | Denial of Service | deployed endpoint | mitigate | Checkpoint step 6 actively confirms the 429 throttle fires on the live endpoint before sign-off |
| T-01-SSE | Spoofing/Bypass | SSE transport path | mitigate | Checkpoint step 5 confirms the real SSE path is covered by the middleware matcher; mismatch is corrected in plan 03 |
</threat_model>

<verification>
- `pnpm test` and `pnpm exec tsc --noEmit` both exit 0.
- Human checkpoint confirms Inspector connect (both transports), health call, 429 on breach, and real-client listing (D-09).
</verification>

<success_criteria>
- MCP client connects to /api/mcp via Streamable HTTP and SSE with valid JSON-RPC (MCP-01/02).
- tools/list shows health with name/title/description/inputSchema; health call returns ok (MCP-03).
- Requests over the throttle limit per IP return 429 (MCP-05).
- Full Jest suite green; phase ready for /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/phases/01-mcp-foundation/01-05-SUMMARY.md` when done.
Record the confirmed SSE path and the deployed endpoint URL for the Phase 5 docs page.
</output>

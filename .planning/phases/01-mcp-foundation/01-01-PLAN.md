---
phase: 01-mcp-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - public/bank-logos/
  - src/lib/__tests__/mcp-tool-error.test.ts
  - src/lib/__tests__/mcp-registry.test.ts
  - src/lib/__tests__/bank-images.test.ts
  - src/lib/__tests__/middleware-rate-limit.test.ts
autonomous: true
requirements: [MCP-04, MCP-05, PERF-01, MCP-03]
must_haves:
  truths:
    - "mcp-handler, @modelcontextprotocol/sdk, and zod@^3 are installed with no dual-version conflict"
    - "public/bank-logos/ contains one PNG per bank code, named per BANK_IMAGE_CODE_ALIASES"
    - "Failing Wave 0 test files exist for tool-error, registry, bank-images, and middleware"
  artifacts:
    - path: "public/bank-logos/BAI.png"
      provides: "Extracted bank logo proving extraction ran"
    - path: "public/bank-logos/SOL.png"
      provides: "Alias-named logo (BSOL -> SOL) proving alias mapping honored"
    - path: "src/lib/__tests__/mcp-tool-error.test.ts"
      provides: "MCP-04 failing test scaffold"
      contains: "mcpToolHandler"
    - path: "src/lib/__tests__/middleware-rate-limit.test.ts"
      provides: "MCP-05 failing test scaffold"
    - path: "src/lib/__tests__/bank-images.test.ts"
      provides: "PERF-01 failing test scaffold"
  key_links:
    - from: "package.json"
      to: "node_modules/mcp-handler"
      via: "pnpm add"
      pattern: "mcp-handler"
---

<objective>
Lay the Wave 0 foundation every other Phase 1 plan depends on: install the three MCP
packages, extract the inline base64 bank logos into PNG files under `public/bank-logos/`
(while the base64 data still exists in `bank-images.ts`), and create the failing Nyquist
test scaffolds for the MCP error boundary, registry, bank-images path resolution, and the
rate-limit middleware.

Purpose: Downstream plans need the package TypeScript types installed, the PNG assets on
disk (the refactor in plan 04 deletes the base64 source), and test harnesses to verify
against. Per 01-VALIDATION.md, all Wave 0 test files are created here.
Output: Installed deps, populated `public/bank-logos/`, four failing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mcp-foundation/01-RESEARCH.md
@.planning/phases/01-mcp-foundation/01-PATTERNS.md
@.planning/phases/01-mcp-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install MCP packages and verify no version conflict</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (confirm zod/mcp-handler/@modelcontextprotocol/sdk absent)
    - .planning/phases/01-mcp-foundation/01-RESEARCH.md (Standard Stack + Zod Version Note + Package Legitimacy Audit)
  </read_first>
  <action>
    Run `pnpm add mcp-handler@1.1.0 @modelcontextprotocol/sdk zod@^3`. The SDK resolves to
    >=1.26.0 (~1.29.0); zod@^3 resolves to 3.25.x (NOT 4.x — peer dep range). After install
    run `pnpm why zod` and `pnpm why @modelcontextprotocol/sdk` and confirm a single resolved
    version of each (no dual-version tree). All three packages were verified legitimate in the
    RESEARCH.md Package Legitimacy Audit (official Vercel / MCP-spec packages, no postinstall
    scripts) — no per-package legitimacy checkpoint is required.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json').dependencies; if(!p['mcp-handler']||!p['@modelcontextprotocol/sdk']||!p['zod']) process.exit(1); if(!/^[^0-9]*3\./.test(p.zod)) { console.error('zod must be 3.x, got '+p.zod); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `mcp-handler`, `@modelcontextprotocol/sdk`, and `zod`
    - The resolved `zod` version is in the 3.x line (satisfies SDK `^3.25 || ^4.0`)
    - `pnpm why zod` reports a single version (no conflicting dual install)
  </acceptance_criteria>
  <done>Three packages installed, zod is 3.x, no dual-version conflict.</done>
</task>

<task type="auto">
  <name>Task 2: Extract bank logo PNGs into public/bank-logos/</name>
  <files>public/bank-logos/</files>
  <read_first>
    - src/lib/angola/bank-images.ts (still holds ANGOLA_BANK_IMAGE_DATA base64 — read first, it is deleted in plan 04)
    - src/lib/angola/banks.ts lines 55-66 (BANK_IMAGE_CODE_ALIASES definition)
    - .planning/phases/01-mcp-foundation/01-PATTERNS.md (public/bank-logos extraction approach)
  </read_first>
  <action>
    Create `public/bank-logos/` and write one PNG per entry in `ANGOLA_BANK_IMAGE_DATA`.
    Use a one-time Node script (per the extraction approach in 01-PATTERNS.md) that reads
    `ANGOLA_BANK_IMAGE_DATA` and `BANK_IMAGE_CODE_ALIASES`, strips the
    `data:image/...;base64,` prefix from each value, and writes `Buffer.from(base64,'base64')`
    to `public/bank-logos/{alias}.png` where `alias = BANK_IMAGE_CODE_ALIASES[key] ?? key`.
    The alias mapping MUST be honored exactly: BPT->POSTAL.png, BSOL->SOL.png, BVB->BV.png,
    FNB->FINIBANCO.png. Include the PLACEHOLDER entry as `PLACEHOLDER.png`. Do not modify
    `bank-images.ts` or `banks.ts` in this task — only read them and produce the asset files.
    If any base64 value is non-PNG (some are SVG/JPEG per the existing test), still write it
    as `.png` (consumer always looks up `.png`); note any such files in the SUMMARY so plan 04
    / Phase 4 can confirm pdf-lib `embedPng` compatibility.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d='public/bank-logos'; if(!fs.existsSync(d+'/BAI.png')||!fs.existsSync(d+'/SOL.png')||!fs.existsSync(d+'/PLACEHOLDER.png')) process.exit(1); if(fs.readdirSync(d).filter(f=>f.endsWith('.png')).length < 25) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `public/bank-logos/` contains at least 25 `.png` files
    - `BAI.png`, `SOL.png` (alias of BSOL), `BV.png`, `FINIBANCO.png`, `POSTAL.png`, and `PLACEHOLDER.png` all exist
    - No file is named `BSOL.png`/`BVB.png`/`FNB.png`/`BPT.png` (aliases applied, not codes)
  </acceptance_criteria>
  <done>All bank logos extracted to disk under alias-correct filenames; base64 source untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create failing Wave 0 test scaffolds</name>
  <files>src/lib/__tests__/mcp-tool-error.test.ts, src/lib/__tests__/mcp-registry.test.ts, src/lib/__tests__/bank-images.test.ts, src/lib/__tests__/middleware-rate-limit.test.ts</files>
  <read_first>
    - src/lib/__tests__/angola-banks.test.ts (Jest structure analog: describe/it, @/ imports)
    - .planning/phases/01-mcp-foundation/01-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - .planning/phases/01-mcp-foundation/01-RESEARCH.md (Phase Requirements -> Test Map; mcpToolHandler / middleware shapes)
    - src/lib/route-error.ts (RouteError fields for tool-error test)
  </read_first>
  <behavior>
    - mcp-tool-error.test.ts: success returns `{ content: [{type:'text', text}] }`; thrown RouteError returns `{ isError:true }` with text containing the error `code` and `message`; non-RouteError throw returns `isError:true` with code `INTERNAL_SERVER_ERROR`; non-Error throw also maps to `INTERNAL_SERVER_ERROR`.
    - mcp-registry.test.ts: `registerAllTools(mockServer)` causes a tool named `health` to be registered with a non-empty title and description and a Zod `inputSchema` (use a mock server object capturing `registerTool` calls).
    - bank-images.test.ts: `getAngolaBankLogoPath('BAI')` returns a path ending in `BAI.png`; `getAngolaBankLogoPath('NONEXISTENT')` returns `null`.
    - middleware-rate-limit.test.ts: calling the limiter <=60 times for one IP passes through; the 61st returns a 429 whose JSON body is `{ error: { code: 'RATE_LIMIT_EXCEEDED', message } }` and includes a `Retry-After` header; a different IP is not throttled.
  </behavior>
  <action>
    Create the four Jest files listed in files_modified, importing from the (not-yet-existing)
    modules via `@/lib/mcp/tool-error`, `@/lib/mcp/registry`, `@/lib/angola/bank-images`, and
    the project-root `middleware`. Follow the describe/it structure and single-quote/2-space
    conventions from `angola-banks.test.ts`. Tests reference symbols that do not exist yet, so
    they MUST fail (RED) at this point — that is the expected state. For the middleware test,
    simulate requests by constructing minimal `NextRequest`-like objects carrying an
    `x-forwarded-for` header; assert status 429 and the `Retry-After` header on the 61st call.
    Do NOT implement any production module in this task.
  </action>
  <verify>
    <automated>test $(pnpm test -- --listTests 2>/dev/null | grep -cE 'mcp-tool-error|mcp-registry|bank-images|middleware-rate-limit') -ge 4</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist and are discovered by Jest (`--listTests` lists them)
    - Running them fails with module-not-found / undefined-symbol (RED) — proving they test unbuilt code
    - Each file uses `@/` path-alias imports and the existing Jest describe/it conventions
  </acceptance_criteria>
  <done>Four failing Nyquist test scaffolds exist for MCP-03, MCP-04, MCP-05, PERF-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build | Third-party packages enter the supply chain at install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pnpm package installs (mcp-handler, sdk, zod) | mitigate | RESEARCH.md Package Legitimacy Audit verified all three as official packages (Vercel/MCP-spec/colinhacks) with no postinstall scripts; `pnpm why` run post-install to confirm no rogue dual-version |
| T-01-02 | Tampering | extracted PNG assets | accept | Bytes are decoded from existing in-repo base64 already shipped; no new external input |
</threat_model>

<verification>
- `pnpm test -- bank-images mcp-tool-error mcp-registry middleware-rate-limit` runs and the new files are RED (expected pre-implementation).
- `pnpm why zod` shows a single 3.x resolution.
- `public/bank-logos/` populated with alias-correct PNG filenames.
</verification>

<success_criteria>
- Three MCP packages installed, zod pinned to 3.x, no dual-version conflict.
- `public/bank-logos/` contains the full set of alias-named PNGs including PLACEHOLDER.png.
- Four failing Wave 0 test files exist, covering MCP-03/04/05 and PERF-01.
</success_criteria>

<output>
Create `.planning/phases/01-mcp-foundation/01-01-SUMMARY.md` when done.
Note in the SUMMARY: any non-PNG-origin logo files (for Phase 4 embedPng confirmation) and
the resolved versions of all three packages.
</output>

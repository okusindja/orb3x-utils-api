---
phase: 03-external-http-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/tool-error.ts
  - src/lib/__tests__/mcp-tool-error.test.ts
autonomous: true
requirements: [FIN-02, NIF-01, TRN-01]
must_haves:
  truths:
    - "D-01: a thrown Error with string code + number statusCode maps to a structured isError payload"
    - "D-01: RouteError still maps via its own instanceof branch after the extension (regression intact)"
    - "D-01: unknown errors still fall through to INTERNAL_SERVER_ERROR"
    - "D-04: extra enumerable error props (retryable, retryAfterSeconds) are serialized into the isError JSON"
    - "D-02: this foundation lands before any external tool depends on it"
  artifacts:
    - path: "src/lib/mcp/tool-error.ts"
      provides: "mcpToolHandler with duck-typed domain-error branch"
      contains: "statusCode"
    - path: "src/lib/__tests__/mcp-tool-error.test.ts"
      provides: "duck-typed branch + regression coverage"
      contains: "UPSTREAM_TIMEOUT"
  key_links:
    - from: "src/lib/mcp/tool-error.ts duck-typed branch"
      to: "CurrencyError/PortalLookupError/TranslationError (NOT imported)"
      via: "structural typeof check on code+statusCode"
      pattern: "typeof.*statusCode.*number"
---

<objective>
Extend `mcpToolHandler` in `src/lib/mcp/tool-error.ts` with a generic duck-typed
error branch (D-01) so that domain error classes sharing the shape
`{ code: string, statusCode: number }` — `CurrencyError`, `PortalLookupError`,
`TranslationError` — map to a structured `isError` payload WITHOUT being imported.
This is the shared foundation (D-02) every Wave 2 external tool depends on.

Purpose: Centralize upstream-error mapping at the single MCP boundary, keeping the
infra layer domain-agnostic. The existing Phase 1/2 RouteError mapping must remain
byte-for-byte behaviorally unchanged (regression).
Output: Modified `tool-error.ts` + extended `mcp-tool-error.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-external-http-tools/03-RESEARCH.md
@.planning/phases/03-external-http-tools/03-PATTERNS.md

<interfaces>
RouteError (src/lib/route-error.ts): fields are `code: string`, `status: number`,
`details?: Record<string, unknown>` — NO `statusCode` property. The duck-typed
check (`statusCode: number`) is therefore disjoint from RouteError.

CurrencyError / PortalLookupError / TranslationError: each `extends Error` with
public readonly `code: string` and `statusCode: number` (constructor signature
`(message, statusCode, code)`). All three are enumerable instance props.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the duck-typed domain-error branch to mcpToolHandler</name>
  <files>src/lib/mcp/tool-error.ts, src/lib/__tests__/mcp-tool-error.test.ts</files>
  <read_first>
    - src/lib/mcp/tool-error.ts (current 45-line implementation — the extension point)
    - src/lib/route-error.ts (confirm RouteError uses .status, not .statusCode)
    - src/lib/__tests__/mcp-tool-error.test.ts (4 existing cases — the regression baseline)
    - 03-PATTERNS.md section "src/lib/mcp/tool-error.ts — MODIFY" (exact branch to insert)
    - 03-RESEARCH.md "Pattern 1" and "Pitfall 1"
  </read_first>
  <behavior>
    - A thrown Error carrying `code: 'UPSTREAM_TIMEOUT'` (string) and `statusCode: 504` (number) returns `{ isError: true }` with parsed JSON containing `code`, `message`, `statusCode`.
    - The same path spreads extra enumerable props: a thrown Error with `retryable: true` and `retryAfterSeconds: 5` set via Object.assign serializes those fields into the isError JSON (D-04).
    - A thrown `RouteError('NOT_FOUND', 'Resource not found', 404)` still maps via its own instanceof branch: parsed.code === 'NOT_FOUND' and parsed.statusCode is undefined (RouteError has no statusCode).
    - A thrown plain Error with no code/statusCode still maps to `INTERNAL_SERVER_ERROR`.
    - All 4 pre-existing test cases remain green unchanged.
  </behavior>
  <action>
    Insert a new branch in `mcpToolHandler` AFTER the existing `instanceof RouteError`
    block (~line 32) and BEFORE the final `INTERNAL_SERVER_ERROR` fallback. The
    RouteError check MUST stay first (Pitfall 1). Do NOT add any imports of domain
    error classes — the branch is purely structural per D-01.

    The branch condition: `error instanceof Error` AND `typeof (error as { code?: unknown }).code === 'string'`
    AND `typeof (error as { statusCode?: unknown }).statusCode === 'number'`. Inside, narrow
    to `error as Error & { code: string; statusCode: number }` and return
    `{ isError: true, content: [{ type: 'text', text: JSON.stringify({...}) }] }`.

    The serialized object MUST contain `code`, `message`, `statusCode` from named
    accessors, PLUS a spread of all other enumerable own-properties via
    `Object.fromEntries(Object.entries(domainError).filter(([k]) => !['name','stack','message','code','statusCode'].includes(k)))`.
    This carries `retryable` and `retryAfterSeconds` (set by Wave 2 tool callbacks via
    Object.assign) into the payload while staying domain-agnostic (D-04). Note that
    `message`, `name`, `stack` are non-enumerable on Error.prototype and excluded
    anyway; the explicit filter is defensive (Pitfall 4).

    Then extend `src/lib/__tests__/mcp-tool-error.test.ts` with the new cases from the
    <behavior> block, mirroring the existing `it(...)` shape. Simulate domain errors
    with `Object.assign(new Error('...'), { code, statusCode, ... })` — do NOT import
    CurrencyError/PortalLookupError/TranslationError into the test. Keep the 4 existing
    cases unchanged. Single quotes, 2-space indent, named accessors (CLAUDE.md).
  </action>
  <verify>
    <automated>pnpm test -- mcp-tool-error</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- mcp-tool-error` passes including all 4 prior cases (regression).
    - New case: duck-typed `{code:'UPSTREAM_TIMEOUT', statusCode:504}` Error → isError true, parsed.code/statusCode/message present.
    - New case: extra props `retryable:true, retryAfterSeconds:5` appear in parsed JSON.
    - New case: RouteError still maps via instanceof branch (parsed.statusCode undefined).
    - No import of any domain error class added to tool-error.ts.
  </acceptance_criteria>
  <done>mcpToolHandler maps duck-typed domain errors to structured isError with extra-prop spread; RouteError + unknown-error behavior unchanged; full mcp-tool-error suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream client → mcpToolHandler | Domain error objects (from currency/nif/translate clients) cross into the MCP serialization layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | error JSON serialization in mcpToolHandler | mitigate | Serialize only `code`, `message`, `statusCode`, and explicitly-allowed enumerable props; `stack`/`name` are non-enumerable and filtered out — no stack traces leak to clients |
| T-03-02 | Tampering | duck-typed branch matching | mitigate | RouteError instanceof check stays first; structural check on `statusCode:number` is disjoint from RouteError (uses `.status`), preventing branch-ordering regressions |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 installs zero new packages (RESEARCH "No Package Legitimacy Audit Required"); no install task in this plan |
</threat_model>

<verification>
- `pnpm test -- mcp-tool-error` green (existing 4 + new cases).
- `pnpm exec tsc --noEmit` clean.
- No domain-error-class import present in `src/lib/mcp/tool-error.ts`.
</verification>

<success_criteria>
mcpToolHandler has a domain-agnostic duck-typed branch (D-01) that maps
`{code,statusCode}` errors to structured isError, spreads extra enumerable props
(D-04 fields), and leaves RouteError + INTERNAL_SERVER_ERROR behavior intact (D-02
foundation ready for Wave 2).
</success_criteria>

<output>
Create `.planning/phases/03-external-http-tools/03-01-SUMMARY.md` when done.
</output>

---
phase: 03-external-http-tools
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/mcp/tools/finance.ts
  - src/lib/__tests__/mcp-tools-finance.test.ts
autonomous: true
requirements: [FIN-01]
must_haves:
  truths:
    - "FIN-01: client can call finance_vat and receive an Angola VAT breakdown as JSON text"
    - "FIN-01: client can call finance_invoice_total and receive subtotal/VAT/grand-total breakdown"
    - "FIN-01: client can call finance_inflation_adjust and receive an inflation-adjusted amount"
    - "Finance tools are PURE — no timeout, no retry, no cache (carried-forward Phase 2 pattern)"
    - "Bad input (RouteError) returns isError via the existing mcpToolHandler RouteError branch (D-01 disjoint)"
  artifacts:
    - path: "src/lib/mcp/tools/finance.ts"
      provides: "registerFinanceTools (3 tools)"
      exports: ["registerFinanceTools"]
    - path: "src/lib/__tests__/mcp-tools-finance.test.ts"
      provides: "FIN-01 coverage (registration, happy, 3 error cases)"
      contains: "INVALID_RATE"
  key_links:
    - from: "src/lib/mcp/tools/finance.ts"
      to: "src/lib/angola/finance.ts"
      via: "named import of calculateVat/calculateInvoiceTotals/adjustForInflation"
      pattern: "from '@/lib/angola/finance'"
---

<objective>
Create `src/lib/mcp/tools/finance.ts` exposing 3 PURE MCP tools (FIN-01):
`finance_vat` → `calculateVat`, `finance_invoice_total` → `calculateInvoiceTotals`,
`finance_inflation_adjust` → `adjustForInflation`. These behave exactly like Phase 2
tools — pure delegation through `mcpToolHandler`, no timeout/retry/cache. Finance
errors are `RouteError` and flow through the existing RouteError branch (unaffected by
the D-01 duck-typed extension, which is disjoint).

Purpose: Wrap the existing pure finance domain logic as MCP tools.
Output: `finance.ts` tool module + `mcp-tools-finance.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-external-http-tools/03-RESEARCH.md
@.planning/phases/03-external-http-tools/03-PATTERNS.md

<interfaces>
calculateVat({ amount, rate = 14, inclusive = true }) — pure; throws RouteError
INVALID_RATE when rate ∉ [0,100]. Returns netAmount, vatAmount, grossAmount.
calculateInvoiceTotals({ lines, discount = 0, discountType = 'amount' }) — pure;
throws INVALID_INVOICE_LINES (empty lines), INVALID_INVOICE_LINE (qty≤0 / unitPrice<0).
adjustForInflation({ amount, from, to }) — from/to are STRINGS (sliced internally,
NOT numbers); throws UNSUPPORTED_CPI_YEAR / INVALID_YEAR.
mcpToolHandler(fn) — wraps callback; serializes return as JSON text; maps RouteError.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement registerFinanceTools (3 pure tools)</name>
  <files>src/lib/mcp/tools/finance.ts, src/lib/__tests__/mcp-tools-finance.test.ts</files>
  <read_first>
    - src/lib/mcp/tools/salary.ts (EXACT template — register pattern, imports, anti-collision descriptions)
    - src/lib/__tests__/mcp-tools-salary.test.ts (test template — registration, handler invocation, error cases)
    - src/lib/angola/finance.ts (signatures + RouteError throw codes for calculateVat/calculateInvoiceTotals/adjustForInflation)
    - app/finance/vat/route.ts, app/finance/invoice-total/route.ts, app/finance/inflation-adjust/route.ts (HTTP parser shapes for Zod derivation)
    - src/lib/mcp/tool-error.ts (mcpToolHandler boundary)
    - 03-PATTERNS.md sections "src/lib/mcp/tools/finance.ts" and "mcp-tools-finance.test.ts"
    - 03-RESEARCH.md "Pattern 2" + "Pitfall 5"
  </read_first>
  <behavior>
    - registerFinanceTools registers finance_vat, finance_invoice_total, finance_inflation_adjust on the server.
    - finance_vat happy path: { amount:114, rate:14, inclusive:true } → isError undefined; parsed has netAmount + vatAmount.
    - finance_vat bad rate: { amount:114, rate:150 } → isError true; parsed.code === 'INVALID_RATE'.
    - finance_invoice_total empty lines: { lines: [] } → isError true; parsed.code === 'INVALID_INVOICE_LINES'.
    - finance_inflation_adjust unsupported year: { amount:1000, from:'2010', to:'2025' } → isError true; parsed.code === 'UNSUPPORTED_CPI_YEAR'.
    - Each tool description names at least one sibling tool by backtick-name (anti-collision).
  </behavior>
  <action>
    Create `src/lib/mcp/tools/finance.ts` exporting `registerFinanceTools(server: McpServer): void`,
    mirroring `src/lib/mcp/tools/salary.ts` exactly. Imports: `z` from 'zod', type `McpServer`
    from '@modelcontextprotocol/sdk/server/mcp.js', `mcpToolHandler` from '../tool-error',
    and `calculateVat, calculateInvoiceTotals, adjustForInflation` from '@/lib/angola/finance'.

    Register three tools via `server.registerTool(name, meta, mcpToolHandler(async (input) => fn(input)))`:
    - `finance_vat`: inputSchema `{ amount: z.number().positive(), rate: z.number().min(0).max(100).optional().default(14), inclusive: z.boolean().optional().default(true) }` with per-field `.describe()`. Title "Angola VAT Calculator".
    - `finance_invoice_total`: inputSchema `{ lines: z.array(z.object({ description: z.string().optional(), quantity: z.number().positive(), unitPrice: z.number().nonnegative(), vatRate: z.number().min(0).max(100).optional().default(14) })).min(1), discount: z.number().nonnegative().optional().default(0), discountType: z.enum(['amount','percent']).optional().default('amount') }`. Title "Angola Invoice Totals".
    - `finance_inflation_adjust`: inputSchema `{ amount: z.number().positive(), from: z.string(), to: z.string() }`. CRITICAL: from/to are `z.string()` NOT z.number — adjustForInflation slices them as strings (Pitfall 5). Title "Angola Inflation Adjustment".

    Use `z.number()` NOT `z.coerce` (carried-forward convention). Every tool description
    must reference at least one sibling tool by backtick-name (e.g. finance_vat mentions
    `finance_invoice_total` and `finance_inflation_adjust`). Do NOT catch inside callbacks
    — finance tools are pure; RouteError propagates to mcpToolHandler. No timeout, no cache.

    Create `src/lib/__tests__/mcp-tools-finance.test.ts` mirroring `mcp-tools-salary.test.ts`:
    a registration test (mock server captures meta), an anti-collision description test, a
    happy-path handler test, and the 3 error-code tests from <behavior>. No global.fetch mock
    (pure tools). Single quotes, 2-space, named exports (CLAUDE.md).
  </action>
  <verify>
    <automated>pnpm test -- mcp-tools-finance</automated>
  </verify>
  <acceptance_criteria>
    - tools/list (registration test) includes finance_vat, finance_invoice_total, finance_inflation_adjust.
    - finance_vat happy path returns JSON with netAmount/vatAmount; isError undefined.
    - INVALID_RATE, INVALID_INVOICE_LINES, UNSUPPORTED_CPI_YEAR each return isError true with matching code.
    - from/to use z.string() (no z.number) for finance_inflation_adjust.
    - `pnpm test -- mcp-tools-finance` green.
  </acceptance_criteria>
  <done>registerFinanceTools registers 3 pure tools; happy path and 3 RouteError cases verified; finance test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → finance tool input | Untrusted typed JSON (amounts, rates, years) crosses into pure domain functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-F1 | Tampering | finance tool inputSchema | mitigate | Zod validation (positive/min/max/enum) at tool entry; finance.ts sanitizers throw RouteError for out-of-range values (double-validated, ASVS V5) |
| T-03-F2 | Denial of Service | finance_invoice_total lines array | mitigate | z.array(...).min(1) bounds the input; pure synchronous computation, no upstream/IO; no unbounded loop risk |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 installs zero new packages; no install task in this plan |
</threat_model>

<verification>
- `pnpm test -- mcp-tools-finance` green.
- `pnpm exec tsc --noEmit` clean.
</verification>

<success_criteria>
3 pure finance MCP tools (FIN-01) registered and callable; happy paths return JSON
breakdowns; invalid inputs return structured isError via the existing RouteError branch.
</success_criteria>

<output>
Create `.planning/phases/03-external-http-tools/03-02-SUMMARY.md` when done.
</output>

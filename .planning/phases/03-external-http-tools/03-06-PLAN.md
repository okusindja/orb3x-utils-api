---
phase: 03-external-http-tools
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - src/lib/mcp/registry.ts
  - src/lib/__tests__/mcp-registry.test.ts
autonomous: true
requirements: [FIN-01, FIN-02, NIF-01, TRN-01]
must_haves:
  truths:
    - "D-05: registry wired LAST as a single integration step (parallel-safety from Phase 2)"
    - "FIN-01/FIN-02/NIF-01/TRN-01: all 7 new tools are discoverable via tools/list through registerAllTools"
    - "Registry includes registerFinanceTools, registerCurrencyTools, registerNifTools, registerTranslationTools"
    - "Full suite green + tsc clean before phase verify"
  artifacts:
    - path: "src/lib/mcp/registry.ts"
      provides: "registerAllTools wiring all 4 new register*Tools"
      contains: "registerFinanceTools"
    - path: "src/lib/__tests__/mcp-registry.test.ts"
      provides: "integration assertion: >= 22 tools incl. the 7 new names"
      contains: "currency_rates"
  key_links:
    - from: "src/lib/mcp/registry.ts registerAllTools"
      to: "tools/{finance,currency,nif,translation} register*Tools"
      via: "import + call after registerSalaryTools"
      pattern: "registerFinanceTools|registerCurrencyTools|registerNifTools|registerTranslationTools"
---

<objective>
Wire the four Wave 2 register*Tools functions into `registerAllTools`
(`src/lib/mcp/registry.ts`) as the final integration step (D-05 — registry edited only
once, last, keeping Wave 2 domain plans disjoint and parallel-safe). Update
`mcp-registry.test.ts` to assert the 7 new tools (3 finance + 2 currency + 1 nif + 1
translation) bringing the total to 22, then run the full suite + tsc gate.

Purpose: Make all Phase 3 tools discoverable via tools/list and lock the canonical tool
catalog. Covers FIN-01, FIN-02, NIF-01, TRN-01.
Output: Modified `registry.ts` + extended `mcp-registry.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-external-http-tools/03-RESEARCH.md
@.planning/phases/03-external-http-tools/03-PATTERNS.md

<interfaces>
Current registry.ts: imports + calls registerHealthTool, registerAddressTools,
registerCalendarTools, registerGeoTools, registerPhoneTools, registerSalaryTools inside
registerAllTools (14 tools today).
New exports from Wave 2: registerFinanceTools (./tools/finance), registerCurrencyTools
(./tools/currency), registerNifTools (./tools/nif), registerTranslationTools (./tools/translation).
mcp-registry.test.ts: existing assertion `Object.keys(registeredTools).length >= 15`
(comment says 14 core) and an enumerated expectedTools array. New total = 22.
7 new tool names: finance_vat, finance_invoice_total, finance_inflation_adjust,
currency_rates, currency_convert, nif_lookup, translate_text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire 4 register*Tools into registry and update the integration test</name>
  <files>src/lib/mcp/registry.ts, src/lib/__tests__/mcp-registry.test.ts</files>
  <read_first>
    - src/lib/mcp/registry.ts (current 16-line file — imports + registerAllTools body)
    - src/lib/__tests__/mcp-registry.test.ts (current count assertion + expectedTools enumeration)
    - 03-PATTERNS.md section "src/lib/mcp/registry.ts — EXTEND" (exact import + call additions, test count update)
    - 03-RESEARCH.md "Open Question 3" (registry test update scope)
  </read_first>
  <action>
    In `src/lib/mcp/registry.ts` add four imports — `registerFinanceTools` from './tools/finance',
    `registerCurrencyTools` from './tools/currency', `registerNifTools` from './tools/nif',
    `registerTranslationTools` from './tools/translation' — and four matching calls inside
    `registerAllTools`, placed after `registerSalaryTools(server)`. Match the existing import +
    call style exactly (single quotes, 2-space, no reordering of existing entries). Do NOT touch
    any tool module — only registry.ts and its test.

    In `src/lib/__tests__/mcp-registry.test.ts`: add the 7 new tool names (finance_vat,
    finance_invoice_total, finance_inflation_adjust, currency_rates, currency_convert, nif_lookup,
    translate_text) to the enumerated expectedTools array, and update the count assertion to
    `>= 22` (and any "14 core utility tools" label to reflect 22). If external-tool registration
    requires a global.fetch shim to import without error, add it; registration is synchronous and
    does not call upstreams, so a mock should not be needed.
  </action>
  <verify>
    <automated>pnpm test -- mcp-registry && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - registry.ts imports and calls all 4 new register*Tools after registerSalaryTools.
    - mcp-registry test asserts >= 22 tools and enumerates all 7 new names.
    - `pnpm test -- mcp-registry` green and `pnpm exec tsc --noEmit` clean.
  </acceptance_criteria>
  <done>All 4 new register*Tools wired into registerAllTools; registry test asserts 22 tools incl. the 7 new names; registry test + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite + tsc phase gate</name>
  <files>(no source changes — verification gate)</files>
  <read_first>
    - .planning/phases/03-external-http-tools/03-VALIDATION.md (sampling rate, before-verify gate)
  </read_first>
  <action>
    Run the full Jest suite and the TypeScript no-emit check to confirm the complete Phase 3
    surface (foundation + 4 domains + registry) is green together with no regressions in the
    Phase 1/2 tools. If any test fails, fix the offending plan's artifact (do not silence tests).
    This is the before-verify gate from 03-VALIDATION.md.
  </action>
  <verify>
    <automated>pnpm test && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Entire Jest suite green (incl. mcp-tool-error, mcp-tools-finance/currency/nif/translation, mcp-registry, all Phase 1/2 tests).
    - `pnpm exec tsc --noEmit` reports zero errors.
  </acceptance_criteria>
  <done>Full suite + tsc green; Phase 3 ready for verify.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registerAllTools → MCP server | Tool catalog assembly; no untrusted runtime input at registration time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-R1 | Tampering | registry wiring drift | mitigate | mcp-registry test enumerates all 22 tool names as a canonical assertion — accidental tool removal/rename fails the build |
| T-03-R2 | Repudiation | tool catalog mismatch vs docs | accept | Phase 5 docs derive the catalog from the registry; no runtime trust impact this phase |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 installs zero new packages; no install task in this plan |
</threat_model>

<verification>
- `pnpm test -- mcp-registry` green; `pnpm test` (full) green; `pnpm exec tsc --noEmit` clean.
- `grep -v '^#' src/lib/mcp/registry.ts | grep -c 'registerFinanceTools\|registerCurrencyTools\|registerNifTools\|registerTranslationTools'` ≥ 8 (4 imports + 4 calls).
</verification>

<success_criteria>
All 7 Phase 3 tools (FIN-01, FIN-02, NIF-01, TRN-01) discoverable via tools/list
through registerAllTools; registry integration test asserts 22 tools; full suite + tsc
green (D-05 single-integration-step wiring).
</success_criteria>

<output>
Create `.planning/phases/03-external-http-tools/03-06-SUMMARY.md` when done.
</output>

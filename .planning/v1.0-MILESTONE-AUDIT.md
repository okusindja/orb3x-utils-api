---
milestone: v1.0
audited: 2026-06-19
status: tech_debt
scores:
  requirements: 23/23
  phases: 5/5
  integration: 5/5 wired (2 test-coverage warnings)
  flows: 3/3
gaps:
  requirements: []   # none unsatisfied — FAIL gate not tripped
  integration:
    - id: WARNING-1
      from: "src/lib/mcp/catalog.ts (MCP_TOOL_NAMES)"
      to: "src/locales/site/docs/*.ts (rendered /docs/mcp table rows)"
      issue: "Catalog↔registry set-equality is test-enforced, but the rendered docs-page rows are hand-authored per locale and NOT machine-tied to MCP_TOOL_CATALOG. A future tool added to registry+catalog could silently fail to appear as a docs row without any test failing. Today all 25 names match in en.ts (verified). Affects DOCS-01/DOCS-02/LOC-01..03."
      severity: warning
    - id: WARNING-2
      from: "mcp-catalog.test.ts"
      to: "non-en locale mcp tables (pt/es/fr/de/zh/ja)"
      issue: "The catalog test only reads getLocalizedDocsPage('en','mcp'). The 6 non-en locale mcp tables are not asserted to keep all 25 rows / correct hrefs. Deep-merge fallback to en mitigates missing rows, but a malformed override could ship a broken link undetected. Affects LOC-01/02/03."
      severity: warning
  flows: []   # all 3 E2E flows complete
tech_debt:
  - phase: 02-core-utility-tools
    items:
      - "Nyquist: VALIDATION.md present but nyquist_compliant: false (PARTIAL) — run /gsd:validate-phase 2 to close."
  - phase: 03-external-http-tools
    items:
      - "Nyquist: VALIDATION.md present but nyquist_compliant: false (PARTIAL) — run /gsd:validate-phase 3 to close."
  - phase: 01-mcp-foundation
    items:
      - "No SECURITY.md — phase not security-audited (Phases 4-5 were)."
  - phase: 02-core-utility-tools
    items:
      - "No SECURITY.md — phase not security-audited."
  - phase: 03-external-http-tools
    items:
      - "No SECURITY.md — phase not security-audited (external HTTP calls — highest-value to audit)."
  - phase: 05-mcp-documentation
    items:
      - "WARNING-1/2: docs-page rows not test-tied to catalog (catalog comment over-claims 'can never silently drift'). Cheap fix: assert en /docs/mcp first-column == MCP_TOOL_NAMES and each href == /docs/${docsSlug}; optionally loop the 7 locales."
  - phase: 01-mcp-foundation
    items:
      - "MCP-05 rate limiting is best-effort (per-instance, no global cap) — deferred to v2 (SEC-01: Vercel Firewall/KV). Documented in REQUIREMENTS.md."
---

# v1.0 Milestone Audit — orb3x-utils-api

**Status:** `tech_debt` — all 23 requirements satisfied, all 5 phases verified, all cross-phase flows WIRED. No critical blockers. Accumulated non-blocking tech debt below should be reviewed before/at close.

## Scores
| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements | 23/23 | All `[x]` in REQUIREMENTS.md; all 5 phase VERIFICATION.md = passed; SUMMARYs list completion |
| Phases | 5/5 | All `disk_status: complete`, all VERIFICATION passed |
| Integration | WIRED | 5/5 cross-phase connections resolve end-to-end; 2 test-coverage warnings (not runtime breaks) |
| E2E Flows | 3/3 | A: MCP discovery+call · B: binary doc + PDF_TOO_LARGE · C: docs discoverability — all complete |
| Tests / Build | green | `pnpm test` 119/25 suites · `pnpm build` exit 0 (incl. /docs/mcp + 28 v1 shims) |

## Cross-Phase Wiring (all WIRED)
1. `registerAllTools` ↔ catalog: 25 tools, set-equality test-enforced (registry ↔ catalog). 
2. Phase-1 endpoint (`/api/mcp`) serves Phases 2–4 tools via `createMcpHandler(registerAllTools)`.
3. Error routing through `mcpToolHandler` consistent across all 11 tool modules (incl. Phase-3 duck-typed + Phase-4 success bypass).
4. Catalog `docsSlug` → real `/docs/[slug]` pages (all 10 slugs exist).
5. v1 HTTP shims ↔ real handlers (28 shims compiled clean).

## Tech Debt (non-blocking, review before close)
**Nyquist coverage** — phases 2 & 3 are `nyquist_compliant: false` (VALIDATION.md exists, not green). Phases 1/4/5 compliant.

| Phase | VALIDATION.md | nyquist_compliant | Action |
|-------|---------------|-------------------|--------|
| 1 | exists | true | — |
| 2 | exists | **false** | `/gsd:validate-phase 2` |
| 3 | exists | **false** | `/gsd:validate-phase 3` |
| 4 | exists | true | — |
| 5 | exists | true | — |

**Security coverage** — only phases 4 & 5 have SECURITY.md. Phases 1–3 were never security-audited (Phase 3, with external HTTP calls, is the highest-value to run).

| Phase | SECURITY.md | Action |
|-------|-------------|--------|
| 1 | missing | `/gsd:secure-phase 1` (optional) |
| 2 | missing | `/gsd:secure-phase 2` (optional) |
| 3 | missing | `/gsd:secure-phase 3` (recommended — external HTTP surface) |
| 4 | verified (0 open) | — |
| 5 | verified (0 open) | — |

**Integration test-coverage fragility (WARNING-1/2)** — the `/docs/mcp` rendered rows are hand-authored per locale and not machine-tied to `MCP_TOOL_CATALOG`; only `en` is asserted. Matches today; could drift silently in future. Cheap fix: assert en table first-column set-equals `MCP_TOOL_NAMES` and hrefs equal `/docs/${docsSlug}` (loop all 7 locales). (Same as Phase-5 code-review WR-01.)

**Known deferred (documented in REQUIREMENTS.md):** MCP-05 rate-limiting is best-effort per-instance (no global cap) → v2 (SEC-01: Vercel Firewall/KV); STRUCT-02 annotations → v2.

## Verdict
No `unsatisfied` requirements → FAIL gate not tripped. The milestone is shippable. The tech-debt items above are optional hardening that can be closed now (cheap: validate-phase 2/3, secure-phase 3, the docs-row test) or accepted into the v2 backlog.
